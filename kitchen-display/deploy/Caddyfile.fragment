# Add this route to the staging Caddy configuration. KDS is an independently
# deployed service; its only shared infrastructure boundary is the reverse-proxy
# network that lets Caddy reach this container by its Compose service alias.
kds.13-140-159-201.sslip.io {
  encode zstd gzip
  reverse_proxy kitchen-display:8080
}
