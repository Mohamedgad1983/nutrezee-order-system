# 03 — A62: Navigator 1.0.12 يعرض كل طلبات السائق (إزالة سقف الـ 100) — 2026-09-07

**الحالة:** DONE + مركّب على تليفون السائق + Verified.
**الأصل:** الاكتشاف الجانبي في التقرير 02 (اختبار A61): التطبيق كان يعرض 100 طلب كحد أقصى بينما Arsad Ali له 115 طلباً ← 15 طلباً غير مرئية. أمر المالك: "حل مشكلة الـ 100 طلب في التطبيق" ثم "كمل لحد ما تركّب الإصدار على التليفون".

## السبب الجذري — Verified
- الـ Fleetbase public API يقطع أي صفحة عند 100 صف (`limit=200` يعيد 100).
- التطبيق (`src/contexts/OrderManagerContext.tsx`) كان يطلب `limit=50` بلا ترقيم صفحات لقائمة اليوم (`scheduled_at=YYYY-MM-DD`) وللطلبات النشطة.

## الإصلاح (repo التطبيق `nutreeze-driver-app`, A34, PR #10, merged `f32b034`) — Verified
- دالة `queryOrderPages` تمشي على الصفحات `page=1..N` (المعامل `page` مدعوم في الـ API، 1-based)، مع إزالة التكرار بمعرّف الطلب والتوقف عند صفحة قصيرة أو صفحة بلا معرّفات جديدة.
- السقوف: الطلبات النشطة 50 × 10 صفحات، قائمة اليوم 100 × 5 صفحات.
- اختبارات: `__tests__/order-manager-query-bounds-test.js` (الـ mock يحترم `page`؛ حالة 115 طلباً على صفحتين). CI أخضر.
- تثبيت `browserslist 4.28.7` و `fast-uri 3.1.6` لتمرير سياسة الثغرات (أدوات بناء فقط).

## مسار الإصدار — Verified
- الوسم `v1.0.12` مدفوع، لكن وظيفة إصدار CI محجوبة ببوابة فحص الأسرار التاريخي (رمز Mapbox قديم بانتظار إلغاء المالك) — لم تُتجاوز.
- البناء الموقّع تم على ماك المالك: كلمتا سر التوقيع قُرئتا من Mac Keychain (`com.nutreeze.driver.android-signing-store` / `-key`) بواسطة المالك نفسه في تيرمينالُه؛ المساعد لم ير أي قيمة سرية. `assembleRelease` نجح في 3m07s.
- التحقق: `aapt` ← `com.nutreeze.driver` versionName 1.0.12 versionCode 13؛ `apksigner` ← شهادة SHA-256 `83ae7ebd80290e3dc92cb95123aab88c9370cd8621fad8a29c4df0aa8c152965` (الهوية الدائمة نفسها).
- التركيب: `adb install -r` فوق 1.0.11 (#12) على realme RMX3760 ← Success، بيانات التطبيق محفوظة والسائق ما زال مسجّل الدخول.

## النتيجة على الجهاز — Verified (13:20 الكويت)
| الشاشة | القيمة | الـ backend (MySQL, نفس اللحظة) |
|---|---|---|
| Dash → Active Orders | **115** | Arsad Ali: 115 طلباً نشطاً اليوم |
| Orders → Monday orders | **115 orders · 230 stops left** | — |
| شارة تبويب Orders | 115 | — |

السائقون فوق السقف القديم اليوم: Nicholas 116، Arsad 115، Ibrahim 109، Amandeep 107 — كلهم يرون قوائمهم كاملة بعد التحديث.

الصور: `img/03_dash_1_0_12_115.png`, `img/03_orders_1_0_12_115.png`.

## ما بقي مفتوحاً
- توزيع 1.0.12 على باقي تليفونات السائقين (المالك). لا يوجد تحديث تلقائي؛ التركيب اليدوي فوق الإصدار الحالي.
- بوابة إصدار CI: تبقى محجوبة حتى يُلغي المالك رمز Mapbox التاريخي ويؤكد بمرجع، فيُسجَّل في `scripts/secret-scan-policy.mjs`.
- البنود 3–8 من التقرير 01 (صورة إثبات، تنبيه الإلغاء بعد Start، webhook، مواقع السائقين) قرارات مالك.
