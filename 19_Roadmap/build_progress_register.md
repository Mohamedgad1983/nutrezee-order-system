# Build Progress Register

**Status:** Living document — updated by every Phase 5 build session (see `phase_5_master_prompt.md` STEP 5).
Created 2026-06-10. WP definitions: `codex_implementation_sequence.md` (WP-01…14) and `phase_5_master_prompt.md` (WP-00, amendment A5).
**This file's §Amendments table is the sole canonical A-id counter** — stale "next free id" notes in earlier documents are superseded.
*(Date note: Phase 2–4 documents carry 2026-06-11 date stamps written one day ahead in error; git history — all commits 2026-06-10 — is authoritative.)*

## Gate snapshot (re-verify live each session — last verified 2026-06-11)

| Gate | State |
|---|---|
| ① DEC-011 stack signed | ✅ SIGNED 2026-06-10 |
| ② DEC-003 MVP cut signed | ✅ **SIGNED 2026-06-10** (`20_Decisions/DEC-003_mvp_cut.md`) |
| ③ R1 remote backup | ✅ CLOSED 2026-06-10 (GitHub, both branches) |
| ④ Staging + CI live | ✅ **BOTH HALVES DONE 2026-06-12.** CI half: green since WP-00; **14 jobs** incl. `docker-validate` (PR #2 merge `9a86ff4`, 2026-06-11). Staging half: ✅ **LIVE** — sponsor provided a VPS (`20_Decisions/NOTE_vps_staging_host.md`); deployed `main` `d69a107` + D7 nginx fix behind Caddy TLS at `https://13-140-159-201.sslip.io`; 13/13 migrations, super admin bootstrapped, **10/10 smoke passed** (checklist §6 evidence). Restore drill ✅ **done 2026-06-14** (latest nightly dump restored to a throwaway DB, schema 13/13 migrations + 62/62 tables + data intact, then dropped) — this gate's WP-14 entry items are now engineering-clear; remaining = sponsor/workshop |
| ⑤ Workshop held / assumption-carry acceptance | ✅ **Assumption-carry recorded 2026-06-10 for WP-07+** (`20_Decisions/NOTE_assumption_carry_wp07_plus.md`, `ASSUMPTION_REGISTER.md`); workshop itself still outstanding and all assumptions stay sponsor-review-required |

**Current operational frontier: WP-KDS-02 is DONE and production-active under A35; WP-OPS-03 is code-merged + CI complete and BLOCKED AT RELEASE under A22.** The Kitchen Display's server-enforced user-to-section correction is proven for all six current Partner sections and has no remaining KDS blocker. The Logistics Manager's unlimited-count reassignment and credential rotation remain release-blocked on protected least-privilege Fleetbase credentials, a named human manager account, deployment, and staging/Navigator proof; ASM-053 continues to block started/current/terminal orders. WP-OPS-01 is production-active under A37–A42 with a rolling 48-hour (+1/+2 day) sync at 01:00 Kuwait. A42 moved the operational freeze boundary from pre-dispatch to actual `started`/`started_at`, so stable Partner lifecycle/timestamp/membership changes reconcile atomically for unstarted integration-owned jobs while started jobs still fail closed. Aug-22 now matches 674 current source orders (500 dispatched/assigned, 174 held) plus 10 audited missing-source tombstones; Aug-23 remains 790 (582 assigned, 208 held), with unique payload/tracking identities throughout and the timer enabled/waiting. The separate A23/A24 read-only snapshot remains isolated with `fleetbase_written=false`. WP-LBL A27–A29 remains software-complete on staging; A40 code now derives a distinct color from immutable Fleetbase driver identity and prints current vehicle plate + phone without changing the black customer barcode. Under A41, nine real plates from the supplied driver workbook are now linked to nine active drivers through Fleetbase's supported API, using phone only as a transient matching key; the two active drivers whose workbook plate cells are blank remain unlinked and fail closed, so A40 production deployment is still blocked. The physical 100 × 70 mm print/camera pilot remains operational follow-up. WP-LOC-A30 staging software and Fleet-Ops screen are deployed, and Android 14 bilingual fail-closed error-path proof is green; physical assigned-driver call/navigation/GPS-capture/persistence UAT and explicit production activation approval remain open. The legacy-replacement engineering queue is otherwise exhausted, while **WP-14 remains blocked on S1/S2 sponsor inputs**.

## Work package status

| WP | Title | Status | Branch / merge commit | Suites green | Amendments | Notes / NC carried |
|---|---|---|---|---|---|---|
| WP-00 | Environment standup | **DONE 2026-06-10** | `build/wp-00-environment-standup` → merge `e704eca` (feat `213478c`) | lint ✅ typecheck ✅ build ✅ placeholder suites 8/8 ✅ + `GET /health` runtime smoke ✅ (all local) | A5 | Staging deferred per A5 carve-out (no PG-region note); compose authored but unvalidated locally (Docker not installed); first GitHub CI run to be verified post-push |
| WP-01 | Platform foundation | **DONE 2026-06-10** | `build/wp-01-platform-foundation` → merge `569dfe1` (feat `fddb1d1`) | Local: 25/25 (TS-I, TS-A #2/#3, TS-R generated 132-case, TS-U auth) + lint/typecheck/build/scans. CI run 27281542344: **13/13 jobs green** incl. DB-backed suites vs postgres:16 service + real boundary/no-GET-mutation scans | — | Wave-1 schema (18 tables) + seeds (12 roles, 48 transition rows [Proposed DEC-005], 13 settings); audit/outbox/idempotency/sessions/staged-RBAC platform. NC carried: DEC-005 enums Proposed; argon2 params pinned at WP-02; audit monthly-partition automation deferred (default partition) |
| WP-02 | RBAC & staff admin | **DONE 2026-06-10** | `build/wp-02-rbac-staff-admin` → merge `2091f7c` (feat `ce9f762`) | Local 37/37 (TS-R full matrix log+warn+deny sweeps; TS-U staff ×10) + lint/scans; CI 13/13 green | — | Staff lifecycle HIGH-audited w/ diffs; grants only via M13 (C4); dormant-grant alert; level caps [Proposed]; matrix export-compare CLI; argon2 params pinned. NC carried: matrix content awaits S8 sign-off; admin SPA screens consolidate at WP-07 UI |
| WP-03 | Settings & transition engine | **DONE 2026-06-10** | `build/wp-03-settings-transition-engine` → merge `4906fbc` | Local 51/51 (TS-U transitions GENERATED from config: 40+ active rules × allow/role/reason + fail-closed validators + no-redeploy config edits; TS-I settings invalidation proven) + CI 13/13 | — | Engine = platform service; owning modules write their own tables via apply callback (ADR-010). Rule CONTENT stays workshop-owned [NC DEC-005/006] |
| WP-04 | Customers | **DONE 2026-06-10** | `build/wp-04-customers` → merge (see git log) | Local 61/61 (TS-U dedup/read-logging; TS-S #6 merge+undo) + CI 13/13 | A1 applied in DDL | Soft-unique phones (family-share NC); merge re-link hook ready for WP-07 drafts; HTTP surface consolidates at WP-07; primary-phone ordering bug caught by partial index in test |
| WP-05 | Catalog | **DONE 2026-06-10** | `build/wp-05-catalog` → merge (git log) | Local 68/68 (TS-U allergen-resolver + mirror-mode DoD) + CI 13/13 | **A6** | Mirror mode proven (import-only core writes until cutover_catalog); cycle guard (C7); routing rules zero-row [NC DEC-006]; catalog owner [NC Q1] |
| WP-06 | Import tooling | **DONE 2026-06-10** | `build/wp-06-import-tooling` → merge (git log) | Local 74/74 (TS-M real: dry-run report, apply gate, idempotency, quality gates, rollback) + CI 13/13 (ts-a rerun after Docker-registry flake) | — | Boundary scan caught 3 ADR-010 violations in first draft → import APIs on owning modules. Real legacy runs still blocked on access/export (WP-13) |
| WP-07 | Intake & WhatsApp panel | **DONE 2026-06-10** | `build/wp-07-intake-whatsapp-panel` → merge `59dfa3c` (feat `074417f`) | Local 83/83 full Vitest + lint/typecheck/build/scans; CI run 27289272120: **13/13 jobs green** | A7, A8 | M01 draft intake/completeness/incomplete queue/allergy warnings + M17 immutable manual WhatsApp refs. Built under ASM-003..024; sponsor review still required for assumptions. |
| WP-08 | Review queue | **DONE 2026-06-10** | `build/wp-08-review-queue` → merge `f68177d` (feat `c22885f`) | Local 93/93 full Vitest + lint/typecheck/build/scans; CI run 27290329062: **13/13 jobs green** | A9 | M02 queue/SLA/claim/decisions, append-only review decisions, return/reject via M01 transition port, approval emits `order.approved` with WP-09 conversion pending. ASM-025 reviewer=creator allow+audit remains sponsor-review-required. |
| WP-09 | Order core | **DONE 2026-06-10** | `build/wp-09-order-core` → merge `5e7bdd9` (feat `82d2121`) | Local 105/105 full Vitest + lint/typecheck/build/scans; CI run 27293379729: **13/13 jobs green** | A10 | M03 OrderFactory converts approved drafts, freezes package/item/allergen/address snapshots, generates fulfillment days, runs plan/day transitions via `transition_config`, supports end-date change requests and exception cases. Built under ASM-026/027 and ASM-043..046; sponsor review still required. |
| WP-10 | Kitchen | **DONE 2026-06-10** | `build/wp-10-kitchen` → merge `df5441d` (feat `35c3b37`) | Local 115/115 full Vitest + lint/typecheck/build/scans; branch CI run 27294417531: **13/13 jobs green**; merge CI run 27294563551: **13/13 jobs green after rerun of infra-only Docker pull flake** | A11 | M08 kitchen ticket generation, board, shared-device ticket transitions, day rollups, pack confirmation, allergy markers, unrouted tickets, and escalations. Built under ASM-028..030 and ASM-047..048; sponsor review still required. |
| WP-11 | Payments-lite | **DONE 2026-06-10** | `build/wp-11-payments-lite` → merge `f551a77` (feat `162ebe9`) | Local 128/128 full Vitest + lint/typecheck/build/scans; branch CI run 27295496661: **13/13 jobs green**; merge CI run 27295575948: **13/13 jobs green** | A12 | M07 payment records, manual link-sent capture, finance status-review queue, FI-only payment decisions via `transition_config`, HIGH audit/outbox, masked payment reads, legacy-unmapped queue port, and refund `not_enabled` stubs. Built under ASM-019..022, ASM-031..032, ASM-049; sponsor review still required. |
| WP-12 | Notifications & reports | **DONE 2026-06-10** | `build/wp-12-notifications-reports` → merge `9768842` (feat `1136886`) | Local 138/138 full Vitest + lint/typecheck/build/scans; branch CI run 27296556204: **13/13 jobs green**; merge CI run 27296643148: **13/13 jobs green** | A13 | M11 internal/email-capable notification templates, configurable trigger map, append-only/deduped notification log, and router; M15 intake funnel/daily ops/kitchen day-list projections, audited JSON export, and replay-equality harness. Customer notification channels remain dormant under ASM-033; ASM-034..035 remain sponsor-review-required. |
| WP-13 | Bridge & cutover tooling | **DONE 2026-06-10** | `build/wp-13-bridge-cutover` → merge `369266f` (feat `afb15e8`) | Local 147/147 full Vitest + lint/typecheck/build/scans; branch CI run 27297504061: **13/13 jobs green**; merge CI run 27297588422: **13/13 jobs green** | A14 | M18 reconciliation records/diffs/alerts and cutover flag audit; M19 active-plan importer, synthetic dry-run/apply/rollback, API surface, owner-module import/rollback ports, unknown payment status finance queue, unverified off-days/address markers. Real legacy apply still needs access/export; ASM-036..038 and ASM-050 remain sponsor-review-required. |
| WP-OPS-01 | Partner daily Fleetbase dispatch + read-only source snapshot | **A37–A42 PRODUCTION ACTIVE; EXACT 48-HOUR HORIZON THROUGH 2026-08-23** | `build/wp-ops-a30-driver-location`; A36 `40bd2ce`; A37 `64df30b`; A42 `99ba1c0` | Snapshot shell 8/8 ✅; rolling-sync shell 9/9 ✅; PHP syntax + self-test 38/38 ✅; full Vitest 72 files / 405 tests ✅; push + PR CI `32476560894` / `32476563957` both 14/14 ✅; live Aug-22 source 674/674 + DB reconciliation 684 orders/payloads/tracking including 10 tombstones + 0 duplicates ✅ | A18–A20, A23–A24, A36–A39, A42; ASM-057, ASM-058 | A42 permits atomic refresh/reassignment/hold/cancellation/tombstoning only while an integration-owned job is unstarted; `started`/`started_at` freezes source/routability/driver and still fails closed. **Aug 22:** 500 dispatched/assigned, 174 current held, 10 audited missing-source tombstones, 0 started. **Aug 23:** 582 assigned, 208 held. The enabled 01:00 Kuwait timer refreshes +1/+2 independently inside its 00:45–01:45 guard; `Restart=no` remains. The read-only snapshot and A30 activation remain separate. |
| WP-OPS-02 | Logistics Manager driver credential rotation | **BLOCKED AT RELEASE 2026-07-22 — code merged + CI complete** | PR #39 → merge `0124f60` | Local typecheck/lint/build ✅; Vitest 59 files / 300 tests ✅; boundary + no-GET-mutation scans ✅; CI runs 30169312557 + 30169313599 **14/14 ✅**; staging Playwright pending | A21 | Nutrezee-only admin capability lists minimum masked Fleetbase driver data and rotates a selected driver's password. Dedicated fail-closed Logistics Manager role/grant; target user derived server-side from a `driver_*` resource; HIGH secret-free audit ledger; no arbitrary user UUID, password persistence/log/email/response, vendor change, `/legacy` change, deployment, or live credential mutation. Release blocker: protected least-privilege Fleetbase token + named manager account + staging Playwright/Navigator proof. |
| WP-OPS-03 | Unlimited driver-to-driver order reassignment | **BLOCKED AT RELEASE 2026-07-25 — code merged + CI complete** | PR #40 → merge `2e255c1` | Local typecheck/lint/build ✅; Vitest 62 files / 313 tests ✅; boundary + no-GET-mutation scans ✅; snapshot guards 6/6 ✅; local browser QA ✅; CI runs 30169477421 + 30169478994 **14/14 ✅**; staging pending | A22; ASM-053 | Logistics Manager receives all current Nutrezee logistics permissions and may select any number of eligible orders from one Fleetbase driver for batched reassignment to another. Public ids only in browser; UUIDs derived server-side; chunks of 100; concurrent same-order attempts fail closed; HIGH batch/item outcome ledger; no customer/payload PII. Started/current/terminal orders blocked pending a coherent upstream handover API. No vendor or `/legacy` changes, deployment, or live reassignment during build. Release blocker: protected least-privilege order-manager token + named manager account + staging/Navigator proof. |
| WP-LBL-A27/A28/A29/A40 | Legacy daily label + permanent barcode + Fleetbase collection + driver box identity | **A40 CODE + CI COMPLETE; LIVE VEHICLE DATA 9/11 — PRODUCTION BLOCKED ON 2 MISSING PLATES** | A29 `0147c86`; A40 `52bfaa8`; PR #47 | Full Vitest 72 files / 405 tests ✅; focused A40 45/45 ✅; lint/typecheck/build/boundary/no-GET scans ✅; snapshot guards 8/8 ✅; push + PR CI `31955378733` / `31955380616` both 14/14 ✅; A41 live Fleetbase reconciliation 9 vehicles / 9 unique plates / 9 unique driver links / 0 duplicates ✅ | A27–A29, A40–A41 | `ops.nutreeze.com` remains the sole admin and Fleetbase remains current-day order/assignment authority. A40 never uses mutable driver names: it assigns distinct colors by current company driver public ids and prints live Fleetbase vehicle plate + phone in a color band while Code 128 remains black. Under A41, nine workbook plates were linked to the matching active drivers via the supported Fleetbase API; phone was transient match evidence only and is not stored as a plate. Ahmed Al-Salem and Unit-01 remain unlinked because their workbook plate cells are blank, so their labels fail closed and deployment remains blocked. Existing Partner-v2 nutrition and physical 100 × 70 mm print/camera pilot gates remain. |
| WP-LOC-A30 | Assigned-driver missing-location recovery | **STAGING SOFTWARE + FLEET-OPS + BILINGUAL EMULATOR ERROR PATH VERIFIED 2026-08-08 — PR #47 OPEN; physical assigned-driver flow pending** | `build/wp-ops-a30-driver-location`; commits `3664a32`, `3eace32`, `7e00c93`, `7253cc7`, `5dbb1dc`; driver `e6c27b6`; PR #47 | Local Vitest 72/72 files, 402/402 tests; driver A30 10/10 + full Jest 65 suites / 339 tests ✅; lint/typecheck/build/scans ✅; PHP syntax + self-test 26/26 ✅; shell syntax ✅; main CI runs 31257248945 + 31257251333 both 14/14 ✅; driver CI 31257188765 install/tests + Android build ✅; migration/API/Console/auth-boundary/browser + Android 14 bilingual error-state/final-artifact cold-launch smoke ✅ | A30 | Fleetbase-authenticated current assignments only. Migration 0029, the A30 API and supported Fleet-Ops extension are deployed on staging with verified rollback artifacts; migrations 0024–0026 remain absent. The Fleet-Ops page and Navigator emulator route are bilingual and fail closed without a verified driver. No customer assignment was returned and no capture/correction, dispatch, Partner/legacy write or production activation occurred. A physical current-driver assignment/call/navigation/GPS-capture/persistence proof remains required. |
| WP-14 | Pilot hardening & gate | **BLOCKED — sponsor/workshop inputs** | — | Restore drill ✅; staging + CI gate ✅ | — | Staging is live and restore drill is proven. Remaining entry blockers are S1 legacy export/reconciled migration and S2 workshop pack/sign-offs (DEC-006 routing content, L1/L2 semantics, S8 RBAC, assumptions/UAT values). No eligible engineering unit remains. |
| WP-KDS-01 | Standalone Kitchen Display section totals | **DONE 2026-08-08 — production-active** | PR #46 merge `74a703c`; PR #48 merge `5955054`; `build/wp-kds-01-english-default` → PR #49 merge `3743aea` | Local boundary/typecheck/build ✅; Vitest 6 files / 31 tests ✅; fixture Playwright 2/2 ✅; live protected Playwright 1/1 applicable ✅; npm audit 0 ✅; review threads 18/18 resolved ✅; PR #49 post-merge KDS/root CI 6/6 + 14/14 (`31254271648`, `31254271662`) ✅; live English-default/Arabic-toggle HTTPS browser acceptance ✅ | A31–A33; ASM-055 | Release `3743aea` installed at `/opt/nutrezee-kds/releases/3743aea`; artifact SHA-256 `afb83d997d45924554de724a43d10d7e8e5568edf97b62f4add01a094db25240`; image `sha256:505154f8ea522b178e8dd7cd4797c95a24668f6845099228873feaca904014c0`; live at `https://kds.13-140-159-201.sslip.io`. Independent arithmetic remains 3,178 rows / 3,266 source quantity / 6,532 section-work quantity / 6 sections / 222 meal-portion groups / 0 unrouted; privacy and secret-log scans passed. |
| WP-KDS-02 | User-assigned Kitchen Display section isolation | **DONE 2026-08-08 — production-active** | PR #50 merge `251e1f2`; PR #51 merge/final release `0fd988a` | Local boundary/typecheck/build ✅; Vitest 6 files / 34 tests ✅; fixture Playwright 3/3 ✅; npm audit 0 ✅; final post-merge KDS/root CI 6/6 + 14/14 (`31256752632`, `31256752652`) ✅; protected live Playwright 1/1 applicable ✅; six-account API + two-account direct browser isolation ✅ | A35; ASM-055 superseded in part; ASM-056 | Exact artifact `86f596aaeaa15c235a7edc918adbedb7155c9141c27a6224984bb74fd4c93b80`, image `sha256:e30f357f0872d3ace10d36e1dc396b5a979ba3d93a17632add3dce76de0f095c`, active release `/opt/nutrezee-kds/releases/0fd988a`. Accounts `drinks`, `hot`, `pastry`, `salad`, `soup`, and `packing` each returned exactly one matching section; cross-section query 400; health/security/privacy/log gates green. |

Status vocabulary: NOT STARTED · IN PROGRESS (branch open) · BLOCKED (gate/NC — name it) · DONE (merged, suites green, pushed).

## Run log

| Date | Mode | Result |
|---|---|---|
| 2026-06-10 | Mode A — WP-00 | DONE (merge `e704eca`); suites green locally; staging deferred (region NC); WP-01 declared BLOCKED |
| 2026-06-10 | **Sprint Mode** (first run, after Mode B added @ `1ac4876`) | STEP 0 re-verified live: ①③ ✅, ② ❌, ⑤ ❌, ④ ◐. WP-00 already DONE (artifacts verified). Next eligible per dependency diagram = WP-01 → entry gate fails (② DEC-003, ⑤ workshop/NC-carry) → sprint stopped honestly, **0 new WPs executed**, no eligible WPs remain. Unblock = sign DEC-003 + record NC-carry note (or hold workshop) + PG-region note. |
| 2026-06-10 | Sponsor unblock pack (docs only) | DEC-003 SIGNED; NC-carry acceptance recorded (WP-01–06); interim PG staging region noted (AWS me-south-1). Gate re-check: ①②③⑤ ✅, ④ ◐ (CI ✅; staging not provisioned). **WP-01 still blocked on ④ staging-live only.** No WP executed (per session mission). |
| 2026-06-10 | Sponsor DEC-014 staging gate re-scope (docs only) | Staging-live moved to hard pre-pilot/WP-14 entry for WP-01–06; build proceeds on local + CI (CI mandatory). Gate re-check: all gates pass for WP-01–06 → **WP-01 ELIGIBLE**. Staging remains NOT provisioned (credentials blocker stands for provisioning only). No WP executed (per session mission). |
| 2026-06-10 | **Sprint Build Mode** (second run) | **WP-01→02→03→04/05→06 ALL DONE in one session** — 6 atomic WPs, each branch→scope→suites→green-CI→merge→register→push. 74 tests (12 files) green; 6 CI runs 13/13 (one ts-a Docker-registry flake re-run, never merged past red). Local PostgreSQL 16 installed for DB-backed suites. Amendment A6 raised (ingredient_allergen). Boundary scan caught 3 real ADR-010 violations mid-sprint (fixed via owning-module import APIs). Sprint stopped honestly at WP-07's workshop NC (stop-rule; NC-carry ends at WP-06). |
| 2026-06-10 | Discovery Mode - Legacy-first section alignment audit | Audit created at `19_Roadmap/legacy_first_section_alignment_audit.md`. Result **PASS_WITH_GAPS**: old-system section evidence is sufficient for a baseline map, current build coverage is foundation-heavy through WP-06, and WP-07 remains blocked on the mandatory intake-field set/workshop S3 Q8. No WP statuses changed; no feature code/tables/APIs/UI created. |
| 2026-06-10 | Discovery Mode - WP-07 `/orders/create` legacy review pack | Pack created at `22_Meeting_Notes/WP07_orders_create_legacy_review_pack.md`. Result **READY_FOR_SPONSOR_SIGNOFF**: old `/orders/create` field categories are documented, but submit blockers/defaults/warnings/customer identity/WhatsApp/allergy/payment/coupon/delivery decisions remain missing. WP-07 stays **BLOCKED**; no feature code/tables/APIs/UI created. |
| 2026-06-10 | Assumption-carry setup for WP-07+ | `ASSUMPTION_REGISTER.md`, `20_Decisions/NOTE_assumption_carry_wp07_plus.md`, and `22_Meeting_Notes/sponsor_review_package_unresolved_business_questions.md` created from sponsor/user directive. Result: WP-07 is eligible under explicit Assumed-for-build values; unresolved questions remain sponsor-review-required. No feature code/tables/APIs/UI created in this doc-only step. |
| 2026-06-10 | **Sprint Build Mode** (WP-07) | **WP-07 DONE** — branch `build/wp-07-intake-whatsapp-panel`, feature `074417f`, merge `59dfa3c`; local full Vitest 83/83 plus lint/typecheck/build/scans green; GitHub CI run 27289272120 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-08 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-08) | **WP-08 DONE** — branch `build/wp-08-review-queue`, feature `c22885f`, merge `f68177d`; local full Vitest 93/93 plus lint/typecheck/build/scans green; GitHub CI run 27290329062 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-09 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-09) | **WP-09 DONE** — branch `build/wp-09-order-core`, feature `82d2121`, merge `5e7bdd9`; local full Vitest 105/105 plus lint/typecheck/build/scans green; GitHub CI run 27293379729 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-10 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-10) | **WP-10 DONE** — branch `build/wp-10-kitchen`, feature `35c3b37`, merge `df5441d`; local full Vitest 115/115 plus lint/typecheck/build/scans green; branch CI run 27294417531 13/13 green; merge CI run 27294563551 13/13 green after rerun of an infra-only Docker pull flake in `ts-m`. Built under `ASSUMPTION_REGISTER.md`; WP-11 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-11) | **WP-11 DONE** — branch `build/wp-11-payments-lite`, feature `162ebe9`, merge `f551a77`; local full Vitest 128/128 plus lint/typecheck/build/scans green; branch CI run 27295496661 13/13 green; merge CI run 27295575948 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-12 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-12) | **WP-12 DONE** — branch `build/wp-12-notifications-reports`, feature `1136886`, merge `9768842`; local full Vitest 138/138 plus lint/typecheck/build/scans green; branch CI run 27296556204 13/13 green; merge CI run 27296643148 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-13 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-13) | **WP-13 DONE** — branch `build/wp-13-bridge-cutover`, feature `afb15e8`, merge `369266f`; local full Vitest 147/147 plus lint/typecheck/build/scans green; branch CI run 27297504061 13/13 green; merge CI run 27297588422 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-14 is now BLOCKED on hard staging-live/smoke-tested entry gate. |
| 2026-06-11 | **Independent review** (Claude, WP-01..13 audit) | **PASS_WITH_FIXES** — branch `review/claude-wp01-wp13-audit`; full report `19_Roadmap/wp01_wp13_independent_review.md`. 15 confirmed issues fixed with tests (1 critical: M01 date off-by-one east of UTC; high: missing read-path masking, kitchen rollup write-skew, end-date-reduction kitchen guard, ADR-010 rollback/read ports, non-transactional dry-run report). Suites grew 147→163, all green + scans/build. 7 findings logged-only (workshop/pre-pilot scope), incl. no-op transition validators and the cancel-cascade engine bypass — both pre-WP-14 items. Amendment A15. |
| 2026-06-11 | **WP-14 staging-readiness prep** (docs only) | **WP-14 still BLOCKED — honest stop, nothing deployed/marked done.** Verified live: zero cloud credentials anywhere (repo sweep + env), Docker absent locally, staging unprovisioned; region note ✅ (me-south-1 interim). Authored `16_Deployment/staging_provisioning_checklist.md` (sponsor inputs STG-1..7, verified env surface, pre-deploy defects D1–D6 — admin-SPA proxy gap, Secure-cookie/TLS, migrations-not-in-image, dead SESSION_SECRET, no deploy job, compose unvalidated — deploy sequence + 10-step smoke runbook) and `19_Roadmap/wp14_blocker_report.md` (29-item readiness matrix: 4 done / 10 possible-now / staging+workshop+sponsor blocked). Baseline re-verified on `main` `bd51afe`: 163/163 + scans + CI 13/13. Single unblock: sponsor supplies STG-1..6. |
| 2026-06-11 | **WP-14 deploy blockers D1–D6 fixed** (same PR #2) | All six fixed without credentials, none invented: D1 nginx SPA-fallback + same-origin API proxy (18 prefixes, `/kitchen` page vs `/kitchen/*` API collision handled) baked into the admin image; D2 TLS posture explicit + `trust proxy` + unit-tested `COOKIE_SECURE` override (defaults unchanged, fail-secure); D3 `migrate` image stage + compose `tools` service for the gated migration/bootstrap steps; D4 `SESSION_SECRET` struck (never read by code); D5 gated `deploy-staging` workflow (GHCR publish on built-in token; deploy job skipped till sponsor vars/secrets, loud-fail till STG-1 chosen); D6 CI `docker-validate` job (compose config + nginx `-t` + full image builds — first-ever validation of the WP-00 Docker artifacts, CI now 14 jobs). Suites 163→164 (cookie-flag test). Nothing deployed; WP-14 not marked complete; staging still needs sponsor inputs STG-1..6 only. |
| 2026-06-12 | **Sprint Build Mode (WP-UI-01)** | **WP-UI-01 DONE pending PR #3 merge** — branch `build/wp-ui-01-shell-login`: login page + central 401 fallback, app shell w/ sidebar (10 sections), SPA routes under `/app/*` (D1-safe), kitchen board preserved, read-only drafts/review-queue/orders lists. 3-lens adversarial review found 3 must-fix (session-expiry error walls, silently-swallowed logout failure on shared tablets, pushState back-button trap) — all fixed pre-push. CI 14/14 green (run 27421998444). Deployed to staging; visible Playwright suite 7/7 green (`tools/e2e-staging/tests/wpui-shell.spec.ts`). Next: WP-UI-02 (intake form, review decisions, orders detail, payment queue). |
| 2026-06-12 | **Staging provisioned + deployed — gate ④ staging-half flipped ✅** | Sponsor provided a VPS (Ubuntu 24.04, 8c/24GB; `20_Decisions/NOTE_vps_staging_host.md` supersedes me-south-1 for staging). Provisioned via new SSH MCP server (`tools/vps-mcp/`): Docker 29.5.3 + compose v5.1.4, ufw 22/80/443. New `docker/compose.staging.yml` (localhost-rebound ports — Docker published ports bypass ufw — secret-store PG password, restart policies, Caddy service) + `docker/Caddyfile` (Let's Encrypt on `13-140-159-201.sslip.io`). Deployed `main` `d69a107`: images built on-host, 13/13 migrations, super admin bootstrapped (idempotent script verified). **D7 found live + fixed:** nginx implicit trailing-slash 301 broke bare `/kitchen` (SPA board) → exact-match location added to `nginx.admin.conf`, rebuilt, retested (checklist §3). **Smoke 10/10 PASS** (checklist §6 evidence). Nightly `pg_dump` cron + first dump verified. Remaining WP-14 entry items: restore drill, workshop/sponsor items per `wp14_blocker_report.md` §4. |
| 2026-06-13 | **Legacy core coverage audit** (Review Mode, docs only) | Sponsor-directed evidence-only audit of the 7 legacy core modules vs current implementation. New `19_Roadmap/Legacy_Core_Coverage_Matrix.md` (per-module backend/API/UI/staging/cutover status, file-cited) + `Legacy_Core_Gap_To_Cutover.md` (shortest-path gap list + prioritized build list + WP sequence). Findings: Orders=B (backend+API done, action UI missing); Customers/Packages/Products/Reports/Settings=C (services done but Customers/catalog HTTP-controller-less, masters routes missing, finance-report parity + content/social/push out of MVP); Subscribers=D (marketing list, off the order-ops path). No module class A. Recommended sequence: **WP-API-01 → WP-UI-02 → WP-UI-03 → WP-UI-04**; sponsor-owned parallel = legacy export access + workshop pack. No WP executed. |
| 2026-06-14 | **WP-14 restore drill DONE → engineering frontier exhausted** (Deployment/UAT Mode, ops) | Verified the nightly backups are **recoverable** — the last pure-engineering WP-14 entry item. Restored the latest dump (`/opt/nutrezee/backups/nutrezee-2026-06-14.sql.gz`, made by `pg_dump | gzip`) into a throwaway DB `nutrezee_restore_drill` via the `nutrezee-vps` MCP with `ON_ERROR_STOP=1` → replayed clean (rc 0). Integrity vs live: `schema_migrations` 13=13 and **62=62 public tables** (schema complete); both staff accounts + 7 customers + the order + 2 products + 144 audit rows present; live counts ≥ the 02:30 snapshot exactly where there's been activity since (audit 144→169, customers 7→9, merge_record 1→2) — a consistent point-in-time backup. Threw the drill DB away; live `nutrezee` untouched. Docs-only (no repo code). **All remaining work is now sponsor/workshop-gated: WP-DATA-01 (real legacy migration, needs export access S1), routing content (DEC-006), RBAC matrix sign-off (S8), the WP-14 UAT/pilot run. Engineering cannot self-unblock further.** |
| 2026-06-14 | **WP-UI-06 per-order payment actions DONE** (Build Mode, UI-only; admin-merged — CI billing-blocked) | WP-UI-02 follow-up: a **Payment** tab on the order detail (`Orders.tsx`). Shows payment status/method/link-ref (`GET /orders/:id/payments`); **Record link sent** (`POST …/payments/link-sent`, payment.link.record); **Request status change → Finance review** (`POST …/payments/status-requests`, payment.review.request) — closes the order-agent → Finance loop. UI-only (m07 endpoints already exist). PR #35 merge `5b5a0fb`; admin tsc/eslint/build clean. Deployed (admin); **visible Playwright 1/1** (`wpui-order-payments`: open order → Payment tab → request status change → 201 → "sent to Finance review") + curl smoke of record-link-sent (unpaid→link_sent, 200). Admin-merged (CI billing-blocked). **Only the WP-14 restore drill remains before the engineering frontier is fully sponsor/workshop-gated.** |
| 2026-06-14 | **WP-UI-05 customer merge UI DONE** (Build Mode, UI-only; admin-merged — CI billing-blocked) | Surfaced the WP-API-02 merge/undo API on the Customers screen — the merge-review item deferred since WP-UI-03a. **UI-only** (no API change; merge/undo + tests shipped in WP-API-02). 'Merge duplicates' panel: `CustomerPicker` (reuses `GET /customers?phone=`) for winner + duplicate → Merge (`POST /customers/merge`) → Undo (`POST /customers/merge/:id/undo`); same-id guard; merged/undone state. PR #33 merge `0e3cd53`; admin tsc/eslint/build clean. Deployed (admin); **visible Playwright 1/1** (`wpui-merge`: self-seed winner+duplicate → merge 201 → undo 200, self-cleaning, zero crashes). Admin-merged (CI billing-blocked). **Next eligible engineering: per-order payment actions (WP-UI-02 follow-up) or WP-14 restore drill — then the frontier is sponsor/workshop-gated.** |
| 2026-06-14 | **WP-UI-04b DONE → WP-UI-04 enrichment editors COMPLETE** (Build Mode, API+UI; admin-merged — CI billing-blocked) | Second WP-UI-04 sub-unit: product **allergen declare**. API: `POST /catalog/products/:id/allergens` (`catalog.enrich`; bypasses mirror mode; 400 w/o `allergen_id`; 404 missing product) → `declareAllergen`; +ts-c-catalog test (10 cases). UI: `AllergenDeclarer` on the product detail — select from the allergen master (`GET /catalog/allergens`) → Declare → resolved-allergens list refreshes; already-declared allergens filtered from the dropdown. Seeded Peanuts/Gluten/Dairy into the allergen catalog master via M19 import. PR #31 merge `ed18791`; api+admin tsc/eslint/build clean; ts-c-catalog 10/10; boundary + no-GET-mutation scans pass. Deployed (api+admin); **visible Playwright 3/3** (`wpui-catalog`: browse + nutrition edit + declare allergen → 200 → appears in resolved list, zero crashes). Admin-merged (CI billing-blocked). **WP-UI-04 enrichment editors done (nutrition + allergens); routing-rule editor remains workshop-gated (DEC-006).** Next eligible engineering: customer merge UI (surface the WP-API-02 merge/undo API), per-order payment actions (WP-UI-02 follow-up), or the WP-14 restore drill. |
| 2026-06-14 | **WP-UI-04a DONE** (Build Mode, API+UI; admin-merged — CI billing-blocked) | First WP-UI-04 sub-unit: product **nutrition enrichment**. API-before-UI — the catalog controller was read-only; added `POST /catalog/products/:id/nutrition` (`catalog.enrich`; enrichment **bypasses mirror mode** so it works with `cutover_catalog=false`; 404 on missing product; upsert; +ts-c-catalog controller test). UI: the Catalog product-detail Nutrition section gained an Edit form (calories/protein/carbs/fat) → save → re-fetch. PR #29 merge `7f911d7`; api+admin tsc/eslint/build clean; ts-c-catalog 8/8; boundary + no-GET-mutation scans pass. Deployed (api+admin); **visible Playwright 2/2** (`wpui-catalog`: browse read-only **+** edit nutrition 537/41/29/11 → 200 → detail shows them, zero crashes) — and this run also confirmed the post-security-cleanup env-var creds (`E2E_EMAIL`/`E2E_PASSWORD`) + rotated admin password work end-to-end. **GitHub Actions still billing-blocked → admin-merged**; local tests + staging Playwright were the gate. Next: WP-UI-04b (allergen declare editor — needs an allergen seeded into the catalog master via M19 import first, then `POST /catalog/products/:id/allergens`). |
| 2026-06-13 | **WP-API-02 DONE** (Build Mode) | Engineering item 5 (post-WP-UI-03). **Customer merge/undo wired.** `MergeService` had no app provider and its draft_order/customer_order re-link steps were test-only — a live merge would deactivate the loser while drafts/orders still pointed at it (orphaned to an inactive customer). Added static `DraftService.customerRelinkStep()` (m01) + `OrderService.customerRelinkStep()` (m03) — SQL in the owning module, boundary-scan clean — registered on MergeService in app.module; exposed `POST /customers/merge` + `/customers/merge/:id/undo` (`customer.merge`, ops-only; routes before `:id` to avoid the param shadow; `MergeError`→400/404/409). Tests: ts-c-customers merge/undo routes (16/16); **new `ts-s-merge-relink`** proves the real draft+order FKs move on merge and restore on undo; boundary + no-GET-mutation scans pass. PR #26 merge `57fc41b`; CI green. Deployed (api); **live merge/undo smoke on staging** ✅ (winner+loser+address → merge: address re-parented to winner, loser inactive → undo: restored; self-merge→400). **Catalog response casing deferred** (the other WP-API-02 note): the shipped WP-UI-03b catalog UI already consumes camelCase, so reconciling now would churn a live screen for no functional gain — logged as low-priority cleanup. Next frontier: WP-UI-04 (catalog enrichment editors — nutrition/allergens work even in mirror mode; routing rules wait on workshop DEC-006). |
| 2026-06-13 | **WP-UI-03c audit DONE → WP-UI-03 COMPLETE** (Sprint Mode, API+UI) | Final WP-UI-03c sub-unit (API-before-UI): read-only audit log. API: `AuditQueryService.list` (filter entity_type/severity/event_type, capped 100, all bound params) + `AuditController GET /audit` (`audit.read`); `before`/`after` shown only to full-visibility callers (pii∧health∧payment), masked (`***`) otherwise; registered in app.module; +2 ts-c tests (read contract + masking). nginx: added `audit` to the admin proxy allow-list (D8-class — fixed proactively, before it bit). UI: `app/apps/admin/src/pages/Audit.tsx` — severity/entity/event filters + expandable before/after detail; Audit log sidebar flipped live + `/app/audit`. PR #24 merge `c7087d8`; CI green (ts-c-audit 2/2 + docker-validate nginx `-t` + cross-module/no-GET-mutation scans); api+admin tsc/eslint/build clean. Deployed (api+admin); **visible Playwright 1/1** (`wpui-audit.spec.ts`: list real events, filter by event_type, expand before/after detail; zero crashes). Clean unit. **🎯 WP-UI-03 COMPLETE — admin parity for the order-ops slice: all 14 sidebar sections now live** (dashboard, intake, drafts, review-queue, orders, customers, payments, kitchen, catalog, exceptions, reports, staff, settings, audit). Next frontier: WP-API-02 (merge/undo wiring + catalog casing), then WP-UI-04. |
| 2026-06-13 | **WP-UI-03c exceptions DONE** (Sprint Mode, API+UI) | Fourth WP-UI-03c sub-unit — first to need an **API-before-UI** step. API: `OrderService.listExceptions` + `GET /orders/exceptions?state=` (gated `order.read` — exceptions are order operational data; deliberately did NOT add `exception.read`, to avoid expanding the RBAC matrix pending workshop sign-off (S2); `notes` PII-masked per grants; route declared **before** `@Get(':id')` to dodge the param-route shadow — the lesson from the settings reason-codes bug; +2 ts-c controller tests for read-contract + notes-masking). UI: exceptions view (`app/apps/admin/src/pages/Exceptions.tsx`; Exceptions sidebar flipped live + `/app/exceptions`) — state-filtered list + resolve open cases with an escalation reason code (`POST /orders/exceptions/:id/resolve`). PR #22 merge `5fcf21f`; CI green (ts-c-orders 7/7 + cross-module-write/no-GET-mutation scans); api+admin tsc/eslint/build clean. Deployed (api **and** admin rebuilt); **visible Playwright 1/1** (`wpui-exceptions.spec.ts`: self-seed an exception via API → list → resolve → confirm resolved; zero client crashes). Clean unit — no defect. Next: WP-UI-03c FINAL — audit query (also needs a read endpoint: `GET /audit`). |
| 2026-06-13 | **WP-UI-03c staff & roles DONE** (Sprint Mode) | Third WP-UI-03c sub-unit: staff & roles admin (`app/apps/admin/src/pages/Staff.tsx`; Staff & roles sidebar flipped live + `/app/staff`; replaces legacy `/users`). Over **existing** endpoints (no API change): Staff tab — `GET /staff` (email/phone PII-masked server-side) list + per-member Manage panel grant/revoke roles (`POST /rbac/grants|revoke`, role list from the matrix) + deactivate (`/staff/:id/deactivate`) + New-staff create (`POST /staff`); Roles tab — read-only RBAC matrix (`GET /rbac/matrix`: role → permission count, dormant flag, expandable perms). PR #20 merge `df95d1a`; CI green; admin tsc/eslint/build clean. Deployed (admin); **visible Playwright 1/1** (`wpui-staff.spec.ts`: list both seeded staff, live grant→revoke round-trip self-cleaning, matrix expand; zero client crashes). **Real bug caught + fixed (`fd7648c`):** the Manage panel's `run()` set busy=true but only cleared it on error, so after a successful grant every panel button stayed disabled — a user could do one action then was stuck. Reset busy in `finally`. (Plus one test-selector fix: a revoked role returns to the grant dropdown as an `<option>`, so the post-revoke assertion was scoped to the roles list.) Next: WP-UI-03c remaining (exceptions view, audit query — both need a small read endpoint added first, per the API-before-UI check). |
| 2026-06-13 | **WP-UI-03c dashboard DONE** (Sprint Mode) | Second WP-UI-03c sub-unit: dashboard overview (`app/apps/admin/src/pages/Dashboard.tsx`; new **first** sidebar entry + `/app/dashboard`; post-login landing left unchanged = kitchen, so the existing e2e sign-in helpers are untouched). Read-only stat cards aggregating already-wired endpoints — M15 report projections (intake-funnel, daily-ops) + live queue counts (`/review-queue?state=waiting`, `/payment-reviews?state=waiting`, `/orders`). Grouped needs-attention / intake-funnel / orders-&-payments; per-source `try/catch` so one failed fetch degrades that card to '—' instead of blanking the page (central 401 handler still fires). **No new API.** PR #18 merge `933060a`; CI green; admin tsc/eslint/build clean. Deployed (admin); **visible Playwright 1/1** (`wpui-dashboard.spec.ts`: group headers + cards render, seeded counts Reviews-waiting=1 / Payments-to-confirm=1, zero client crashes). Clean unit — no defect. Next: WP-UI-03c remaining (exceptions view; staff/RBAC admin + audit query). |
| 2026-06-13 | **WP-UI-03c settings/masters DONE** (Sprint Mode) | First WP-UI-03c sub-unit: settings/masters admin (`app/apps/admin/src/pages/Settings.tsx`; Settings sidebar flipped live + `/app/settings`). Masters tab — view + add area / delivery-slot / delivery-method / kitchen-section (`GET`/`POST /settings/masters/:kind`); Reason-codes tab — view + add by domain (`GET`/`POST /settings/reason-codes`). Reads `settings.read`, adds `settings.update.ops`. Settings-registry / feature-flag (gate-key) editing deliberately out of scope. PR #16 merge `c490bbd`; CI green (incl. ts-c/ts-i settings 17/17); admin tsc/eslint/build clean. Deployed (api **and** admin rebuilt on the VPS); **visible Playwright 1/1** (`wpui-settings.spec.ts`: view seeded area, add area live 201, switch kind to slots, reason-codes tab, add reason code live 201, zero client crashes). **Real backend bug caught + fixed (`0f42161`):** `POST /settings/reason-codes` was a dead route (404) — the generic `@Post(':key')` update route was declared before it, so Nest's in-order matching shadowed it (`key='reason-codes'` → unknown_key). Moved the static route ahead of `:key`; the endpoint had been unreachable since WP-API-01 (tests hit the controller method directly, not HTTP routing). Next: WP-UI-03c remaining (exceptions view / staff+RBAC+audit / dashboard cards). |
| 2026-06-13 | **WP-UI-03b reports DONE** (Sprint Mode) | Third WP-UI-03 sub-unit (second 03b screen): reports admin (`app/apps/admin/src/pages/Reports.tsx`; Reports sidebar flipped live + `/app/reports`). Read-only over M15 projections — intake-funnel / daily-ops / kitchen-day-list (`GET /reports/:name`, each `report.view.*`) + JSON export (`POST /exports`, `report.export`, client-side download). Reports rebuild from the outbox each call; empty sections render as a labelled demo-data gap. PR #13 merge `4dd5a3b` (screen) + **PR #14 merge `1408f60` (fix)**; CI green both; admin tsc/eslint/build clean. Deployed; **visible Playwright 1/1** (`wpui-reports.spec.ts`: intake-funnel + export + daily-ops + kitchen, asserts zero client crashes). **Real bug caught by the e2e + fixed:** switching report tabs briefly paired the new `report` with the previous report's data shape → `undefined.toLocaleString()` white-screened the whole SPA (no error boundary); fixed by keying loaded data to its request + non-number value guards. intake-funnel shows live seeded numbers (4 drafts created, 1 approved, by_channel phone:4). Next: WP-UI-03c. |
| 2026-06-13 | **WP-UI-03b DONE** (Sprint Mode, post-seed) | Second WP-UI-03 sub-unit: catalog browse admin (`app/apps/admin/src/pages/Catalog.tsx`, new `/app/catalog` route + sidebar). Read-only over the M05 read API (`GET /catalog/*`, `catalog.read`): Products tab (active-only filter) → detail with nutrition + resolved allergens; Packages tab → detail (duration / meals-per-day / parent); Masters tab over the 6 catalog kinds. **Read-only by design** — catalog stays import-only until `cutover_catalog` flips (mirror mode). PR #11 merge `955c9f4`; CI 14/14 + CodeRabbit pass; admin tsc/eslint/vite-build clean. Deployed (admin image rebuilt on the VPS via the `nutrezee-vps` MCP + `up -d`); **visible Playwright 1/1** (`wpui-catalog.spec.ts`: products→detail→packages→detail→masters, real, against the seeded catalog — first 03b screen demonstrable thanks to the seed). One test-selector fix (`getByText('Nutrition')` substring-matched the empty-state copy → `{exact:true}`). Next: WP-UI-03c (settings/masters admin, exceptions, staff/RBAC + audit, dashboard) and/or reports. |
| 2026-06-13 | **Staging seeded for UAT** (Deployment/UAT Mode, user-approved) | Resolved the cross-cutting staging-data gap — the intake→review→order→payment chain was empty because no catalog meant no submittable draft. Created a dedicated `uat-seed@nutrezee.local` super-admin (+ `ops_manager` + `order_agent`: the transition engine checks per-transition `allowed_roles` with **no super_admin bypass**, so super_admin alone can't submit a draft / approve / create an order) via the sanctioned `scripts/bootstrap-admin.mjs` in the migrate container — the user's real account was untouched. Seeded over HTTPS via the API: catalog (2 meal_types, 2 products, 2 packages) through the **M19 import** (`/imports/catalog/dry-run`→`/apply`; `via='import'` bypasses mirror mode), ops masters (1 area / slot / method) via `/settings/masters/:kind`, 1 customer + address, then a live chain → 1 order + auto `payment_record` + 1 payment-review waiting, plus 1 review-queue waiting + 2 open drafts. All four daily-ops screens now have live data. `cutover_catalog` still false. Details in memory `staging-uat-seed-data`. |
| 2026-06-13 | **WP-UI-03a DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | First WP-UI-03 sub-unit: customers admin (`app/apps/admin/src/pages/Customers.tsx`, new `/app/customers` route + sidebar — Customers was missing from nav). Search by phone, guided-create (dup block on `duplicate_phone`, warn/force on `possible_duplicate`), profile view (PII/HEALTH masked server-side + masked badge), edit name/email/dob/notes, phones/addresses/allergies display. Replaces legacy `/users/list/3` + customer search in `/orders/create`; merge-review deferred to WP-API-02. PR #10 merge; CI 14/14 (run 27465852463); admin checks clean. Deployed; **visible Playwright 1/1 FULL end-to-end** (`wpui-customers.spec.ts`: create→edit→search, real — first admin screen demonstrable without seed data). Next: WP-UI-03b (catalog read screens, or reports). |
| 2026-06-13 | **WP-UI-02d DONE → WP-UI-02 COMPLETE** (Sprint Mode via "Continue Nutrezee OS Agent") | Final WP-UI-02 sub-unit: Finance payment review queue (`app/apps/admin/src/pages/Payments.tsx`, `/app/payments` flipped live in the sidebar). Queue list (`GET /payment-reviews`, state filter) → decide: approve/confirm with transaction_ref + evidence note, or reject with a `payment_fail` reason code + note (`POST /payment-reviews/:id/decisions`); refunds stay `not_enabled` (WF-13, never auto-confirm). PR #9 merge; CI 14/14 (run 27465680346); admin checks clean. Deployed; **visible Playwright 3/3** (`wpui-payments.spec.ts`). **WP-UI-02 done — all four daily-ops roles now have their screen** (order agent=intake, Ops Manager=review+orders, kitchen=board, Finance=payment queue). Next: WP-UI-03 (admin parity: customers/catalog/reports/settings/staff/audit) — but the highest-value next step is seeding demo data so the intake→review→order→payment chain is clickable for UAT (queue cross-cutting note). |
| 2026-06-13 | **WP-UI-02c DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | Third WP-UI-02 sub-unit: order detail + lifecycle actions (`app/apps/admin/src/pages/Orders.tsx`, `/app/orders` upgraded from read-only list to action screen; superseded `OrdersPage` removed from lists.tsx). Open order → summary (PAYMENT-masked total) + fulfillment-days table; change status (config engine via `POST /orders/:id/transitions`, cancel pulls cancellation reason codes), change request (new end_date diff, WF-15), raise exception (WF-14 taxonomy, allergy_incident auto-HIGH). UI-only — M03 + reason-code APIs already live; server validates every transition/decision. PR #8 merge; CI 14/14 (run 27459064518); admin typecheck/lint/build clean. Deployed; **visible Playwright 3/3** (`wpui-orders.spec.ts`). Full open/act needs an order on staging (seed-data gap, tracked). Next: WP-UI-02d (payment review queue, Finance). |
| 2026-06-13 | **WP-UI-02b DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | Second WP-UI-02 sub-unit: Ops Manager review-queue actions (`app/apps/admin/src/pages/Review.tsx`, `/app/review-queue` upgraded from read-only list to action screen; superseded `ReviewQueuePage` removed from lists.tsx). Queue list → open draft → claim (`POST /review-queue/:id/claim`) → approve (per-warning override reasons) / return / reject with reason code + note (`POST /drafts/:id/decisions`). API-before-UI prerequisite shipped same branch: `GET /settings/reason-codes?domain=` + `SettingsService.listReasonCodes` (reject=rejection, return=return_to_draft domains; `settings.read`). Server enforces reason_code on reject/return + a reason per warning field to approve-past-warnings; UI mirrors both. PR #7 merge; CI 14/14 (run 27458674705); suite 192→193. Deployed; **visible Playwright 3/3** (`wpui-review.spec.ts`): queue loads, state filter, reason-code endpoint wired. Full claim/approve demo needs a submitted draft (staging seed-data gap, tracked). Next: WP-UI-02c (order detail) — or seed demo data to make the intake→review→approve chain clickable. |
| 2026-06-13 | **WP-UI-02a DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | First WP-UI-02 sub-unit: the intake draft form (`app/apps/admin/src/pages/Intake.tsx`, `/app/intake` + sidebar "New intake") — the order agent's daily screen, replaces legacy `/orders/create`. Channel + customer (find-by-phone/guided-create/unverified), package or items (`GET /catalog/*`), dates, area/slot/method (`GET /settings/masters/*`), payment method, address, WhatsApp ref panel; `POST /drafts` (idempotency-keyed) → `GET /drafts/:id` completeness panel → `POST /:id/submit`. PR #6 merge; CI 14/14 (run 27458274619); admin typecheck/lint/build clean. Deployed to staging; **visible Playwright 4/4** (`tools/e2e-staging/tests/wpui-intake.spec.ts`): renders, customer search, guided-create (201), draft create→completeness verdict, draft in list. **Known gap:** full happy-path *submit* needs catalog + ops-master seed data on staging (mirror mode blocks API catalog writes; area/slot/method zero-row until workshop) — create+completeness proven without it. Next: WP-UI-02b (review-queue actions). |
| 2026-06-13 | **WP-API-01b DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | Small API prerequisite for WP-UI-02, detected via the §B decision tree (API-before-UI): the intake draft form needs delivery-slot/method/area dropdowns but WP-API-01 added only POST for ops-masters. Added `GET /settings/masters/:kind` (kind whitelisted; `settings.read`, granted to order_agent so intake can load them; `?active=` filter) + `SettingsService.listMasters` (per-kind trusted column/order map, table name validated against it — kind never user-controlled in SQL). PR #5 merge `22bdcff`; CI 14/14 (run 27457952092); suite 190→192. Deployed + verified on staging (area/slot/method empty on fresh DB, section_master returns 5 seeds, unknown kind→400). **WP-UI-02 intake form now has zero API blockers.** |
| 2026-06-13 | **WP-API-01 DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | First engineering-frontier unit. Branch `build/wp-api-01-...` → PR #4 merge `f9dcae6`. Three HTTP controllers over already-tested services, **no schema change**: M04 CustomerController (search/profile/create/update/address/allergy — PII/HEALTH masked at serialization per visibility grant, health grant-gated), M05 CatalogController (read-only products/packages/masters/allergens/nutrition; new read methods added to CatalogService — reads always allowed, mirror mode gates writes only; master `kind` whitelisted), Settings masters/reason-code POST routes. **3-lens adversarial review caught + fixed pre-merge: (1) HEALTH `diet_status_id` leaked unmasked on profile, (2) PII `full_name_en/_ar/notes`+address fields leaked, (3) 2nd-order SQL injection via `addMaster` column-name keys.** 26 new TS-C tests (incl. regression for all 3); suite 164→190; CI 14/14 (run 27454201633). Deployed to staging; **D8 found live + fixed** (`0c3af5a`): `/customers` + `/catalog` missing from the admin nginx proxy allow-list (D1-class) → SPA fallback instead of API; added both prefixes, rebuilt admin, re-verified (401 unauth + authed `{"items":[]}`). **Deferred (own unit): merge/undo** — MergeService has no app provider and its FK re-link steps are test-only. Fulfils WP-04 "HTTP surface consolidates at WP-07" + WP-03 masters-admin promises. Next: WP-UI-02. |
| 2026-06-13 | **Nutrezee OS Agent operating system created** (Review Mode, docs only) | Repo-level autonomous OS so Claude Code/Codex continue from project state without a new prompt each time. Standing command **"Continue Nutrezee OS Agent"**. Rewrote `AGENTS.md` + `00_AGENT_OPERATING_SYSTEM.md` (preserved source-of-truth table, binding constraints, forbidden-module list; added standing command, session-start sequence, frontier auto-detection, current state). New `19_Roadmap/NEXT_ACTION_QUEUE.md` (live next-work cursor — frontier WP-API-01→UI-02→UI-03→UI-04, sponsor-owned parallel track, deferred list), `19_Roadmap/AUTO_EXECUTION_RULES.md` (session-start, next-work decision tree, assumption logging, stop conditions, mandatory SESSION REPORT), `19_Roadmap/SPRINT_MODE.md` (six modes — Review/Build/Sprint/Fix/Deployment/UAT — + the autonomous loop). No code; no WP executed. Next: WP-API-01. |
| 2026-06-16 | **Legacy full migration workflow** (Review/Build prep; branch `migration/legacy-full-clone-reconciliation`) | Created `docs/evidence/legacy_full_migration/` evidence pack and a dependency-free normalized JSONL validator. Verified repo state, inspected M18/M19 schema/import conventions, documented extraction/transformation/import/reconciliation/browser-verification plans, and recorded an honest blocker. No legacy extraction or new-system import ran: `LEGACY_ADMIN_URL`/DB/API/export vars and `NEW_DATABASE_URL`/E2E target vars were missing. Validator gate is red by design (7 P0 missing normalized files). Local checks green: node syntax, typecheck, lint, build, Vitest 46 files / 207 tests. Next unblock: sponsor supplies full read-only legacy source plus safe staging/local target DB. |
| 2026-06-16 | **Legacy migration RECONCILED → `NOT_VERIFIED_WITH_MISMATCHES`** (resume from `292ae52`; reconcile-only, no import) | The prior "BLOCKED_BY_MISSING_ACCESS" was **local-shell only** — the real migration was already executed on the **staging VPS** (`/opt/nutrezee/` scripts → governed M19 `/imports/active_plans/apply`, 2026-06-14/15). Reconciled the staging clone read-only via `nutrezee-vps` MCP vs the 2026-06-14 legacy extraction; decisive findings independently re-verified by a 5-agent adversarial workflow (1 of my conclusions corrected). **Present & FK-clean (0 orphans):** 19,379 legacy customers, 19,465 orders (active 1,054 / expired 11,162 / rejected 5,710 / cancelled 1,539), 9,506 addresses, 11,257 payments, 7 legacy packages, 127 areas. **P0 gaps:** per-meal order line items not migrated (`order_item`=1, a seed); deliveries not migrated (`site_ref` null all; legacy's 4 delivery methods absent). **P1:** customers 20,151→19,379 = 592 phone-dedup + 29 held + ~151 unprocessed (NOT 772 lost — corrected; risk: family-shared-phone over-merge); 1,732 order-import errors (active_plans) unre-applied; active +10 vs profile; history total unprovable; products only 2; authenticated API/browser verification BLOCKED (no `E2E_*`). Evidence: `docs/evidence/legacy_full_migration/10_…`, `12_final_migration_report.md`, `mismatches.jsonl`. No production/legacy writes, no destructive SQL, no apply (`MIGRATION_APPLY` unset). Local: typecheck/lint/build/Vitest 46f·207t green. Next: obtain staging admin creds + re-extract legacy order-detail/product pages, then corrective re-import (staging, `MIGRATION_APPLY=true`, signed off) + row-level ledger. |
| 2026-07-18 | **Driver password + biometric authentication — BLOCKED at password import** | Workbook mapping verified: 20 source rows → 11 unique drivers, zero invalid phones. Standard Fleetbase API created 11/11 as `available`, but password login verified 0/11: public driver CRUD drops guarded passwords; a real driver token is denied by upstream create-user authorization on `/int/v1/users/change-password`; the admin reset endpoint requires an authenticated console-admin session, and the connected browser is signed out. No vendor or `/legacy` edits; private VPS import material removed. Mandatory security cleanup completed despite the stop: `SMS_AUTH_BYPASS_CODE` removed, zero DB bypass rows, runtime config unset, negative probe HTTP 400, application healthy. Resume when Mohamed signs into `https://ops.nutreeze.com` in the connected browser. |
| 2026-07-19 | **WP-OPS-01 — VERIFIED API SCOPE / BLOCKED UPSTREAM RECONCILIATION** | Reset the 11 existing drivers to administrator-issued simple passwords without changing phones; all 11 external logins returned HTTP 200 with a token. A complete Partner API scan for July 19 produced 954 canonical deliveries. The guarded Fleetbase apply and a second idempotent rerun both exited 0: 954/954 reconciled, 735 valid-pin deliveries assigned/scheduled/dispatched across all 11 drivers, 219 held and unassigned (212 missing pins, 7 invalid), duplicate/unexplained 0, and the rerun created 0 orders/customers/payloads/places/tracking records. Fresh Android 14 emulator proof showed driver `60005678` 142 live July 19 assignments, explicit logout to empty password-first login, and successful relogin. Security recheck: SMS bypass absent + HTTP 400 probe, Fleetbase vendor diff 0, Navigator `/legacy` diff 0, relevant containers healthy. Local suites passed and GitHub CI run 29697810166 finished 14/14 green. **Blocker:** dashboard 981 versus API 954 leaves 27 upstream-missing deliveries; no assumption equates those totals, and the unattended timer remains disabled. |
| 2026-07-20 | **WP-OPS-01 — A19 live apply + bounded Navigator UAT COMPLETE** (Deployment Mode) | Two complete matching Partner walks and the guarded write confirmed the stable 946-row July 20 manifest. Applied 946/946 across 11 drivers: 723 real pins + 223 known-area fallbacks visibly call-required; 0 unknown/country fallbacks, duplicate, held, or unexplained rows. An exact rerun before UAT made 0 creates/updates/tracking/activity writes. The least-loaded driver (30) completed one real-pin/no-POD order through dispatched → started → enroute → completed; July 20 then showed active 945, pending 0, completed 1 in API/dashboard. Security recheck passed: bypass absent + HTTP 400/no token, vendor diff 0, `/legacy` untouched, protected key mode 0600, timer disabled, containers healthy/running. App checks and PHP self-test passed; CI 29749152252 14/14 green. |
| 2026-07-22 | **WP-OPS-02 — driver credential rotation IMPLEMENTED + CI GREEN / BLOCKED AT RELEASE** (Build Mode) | Under sponsor amendment A21, added a dedicated `logistics_manager` role and fail-closed `delivery.driver.credentials.rotate` grant, a bilingual admin driver-password screen, a server-only least-privilege Fleetbase client, server-derived driver→user targeting, and HIGH requested/completed/failed audit evidence without passwords or upstream user UUIDs. Local typecheck/lint/build and full Vitest 59 files / 300 tests passed; cross-module-write and no-GET-mutation scans passed; PR #39 CI run 29920117590 passed 14/14. Fleetbase vendor and Navigator `/legacy` remain untouched; no deployment or live credential change occurred. **Release blocker:** protected Fleetbase service identity/token, named human manager account, and staging Playwright/Navigator proof. |
| 2026-07-22 | **WP-OPS-03 — unlimited driver reassignment IMPLEMENTED + CI GREEN / BLOCKED AT RELEASE** (Build Mode) | Under A22/ASM-053, granted Logistics Manager the exact current delivery permission set and added bilingual unlimited-selection driver-to-driver reassignment through Fleetbase's standard API. Browser submits public ids only; server derives UUIDs, chunks by 100, revalidates source/status per chunk, serializes concurrent attempts, verifies target state, and records HIGH batch plus per-order outcomes without customer/payload PII. Local browser QA showed 2/2 eligible selected while a started order remained blocked; no console errors. Typecheck/lint/build, 62 files / 313 tests, boundary/no-GET scans, and diff checks passed; PR #40 CI run 29922483457 passed 14/14. No deployment, vendor or `/legacy` change, or live reassignment. **Release blocker:** protected least-privilege order-manager token, named human manager account, and staging/Navigator proof. |
| 2026-07-23 | **WP-OPS-01 — A23 daily read-only snapshot DEPLOYED** (Deployment Mode) | Added a write-separated 06:30 Kuwait systemd timer that performs two full Partner `--dry-run` passes, requires matching count+digest, and atomically retains only a 30-day PII-free aggregate manifest. Local shell guards 5/5 plus typecheck/lint/build/Vitest 56 files / 289 tests passed; CI run 29974158252 passed 14/14 including the Linux snapshot guard. Deployed commit `0c1758f` with matching checksums; systemd units verified. Historical live proof for 2026-07-22 completed two matching 529-event walks in ~20 minutes: 873 orders / 3,781 meal rows, 668 real-pin, 198 missing-pin held, 7 invalid-pin held, digest `1579806313c1e8c8ef71a37b4cdcf748f0544aa4a0e957eb92f960f996516b29`; manifest schema allowlist clean, directory/file root modes 0700/0600, `fleetbase_written=false`, raw temporary logs removed. Snapshot timer enabled/active with next run 06:30 Kuwait; dispatch timer independently verified disabled/inactive; Fleetbase app healthy and PHP self-test 23/23. |
| 2026-07-23 | **WP-OPS-01 — A24 snapshot schedule changed to 01:00 Kuwait** (Deployment Mode) | Sponsor changed the enabled read-only snapshot timer from 06:30 to 01:00 Kuwait (`22:00 UTC` on the preceding calendar day). Commit `efc8865`; local schedule/snapshot guards 6/6; CI run 29975600084 passed 14/14. Deployed only the timer unit, verified with `systemd-analyze`, reloaded systemd, and restarted the timer (not the service): the old 06:30 trigger was removed and the next trigger resolved to 01:01:54 Kuwait including randomized delay. Snapshot timer enabled/active; dispatch timer disabled/inactive. The explicit non-authoritative completeness label remains because 01:00 precedes the previously documented 06:00 Partner publication. No source/Fleetbase writes occurred. |
| 2026-07-25 | **WP-OPS-02 + WP-OPS-03 PR CLOSEOUT COMPLETE** (Fix/Review Mode) | Resolved both stacked branches against the current Fleetbase parent without dropping A23/A24. PR #39 merged as `0124f60` after local 300/300 tests and CI runs 30169312557 + 30169313599 passed 14/14. PR #40 was retargeted to `build/partner-daily-fleetbase` and merged as `2e255c1` after local 313/313 tests, snapshot guards 6/6, and CI runs 30169477421 + 30169478994 passed 14/14. Both capabilities remain fail-closed and not deployed pending protected least-privilege Fleetbase credentials, a named Logistics Manager account, migrations 0025/0026 deployment, and visible staging/Navigator proof. No Fleetbase/Partner/legacy write or live credential/order mutation occurred during closeout. |
| 2026-07-25 | **WP-OPS-02 + WP-OPS-03 RELEASE PREFLIGHT — DRAFT PR #43 GREEN / BLOCKED BEFORE DEPLOYMENT** (Deployment Mode) | Re-verified the release gate live. Operational parent `build/partner-daily-fleetbase` is clean and pushed; draft PR #43 targets `main`, is clean/mergeable, and push/PR CI runs 30170042208 + 30170050086 both passed 14/14. Staging is healthy but its Nutrezee source/image predates these features, the database is at 22 applied migrations through `0023_address_block.sql`, migrations `0025/0026` are absent, all three server-side Fleetbase integration variables are unset in the root-owned mode-0600 secret file, and the `logistics_manager` role/account does not exist. The only staff records are the existing Staging Admin and UAT seed account; neither is a dedicated named Logistics Manager. No deployment, migration, account/role grant, Fleetbase/Partner/legacy write, driver credential change, or order reassignment was performed. Exact unblock: provision the two least-privilege Fleetbase service identities/tokens and identify the named human Logistics Manager account; then review/merge PR #43, deploy migrations `0024–0026`, and run staging Playwright/Navigator proof. |
| 2026-07-28 | **WP-LBL-A27/A28 CORRECTION DEPLOYED + BROWSER-VERIFIED** (Fix/Deployment/UAT Mode) | Corrected the A27 product boundary under A28: `ops.nutreeze.com` remains the sole Fleetbase/Fleet-Ops admin; the second `/nz-admin` operations shell is retired; label controls are a supported `Label & Barcode` order-details tab; operators and Navigator reuse Fleetbase identity; driver authority is Kuwait-today Fleetbase assignment. Full pre-change database/image/source backups were verified. Applied only additive migration 0028, deployed API/admin/Console images, and forced all operator browsers past Fleetbase's versioned local/HTTP extension caches. Live synthetic order `order_xn06VY2b1Q` rendered the legacy two-column label plus permanent Code 128 `NZC-GQ2W-Y271-CF`; current-day print history remained zero because Print was not clicked. Routes, API JSON boundary, counts and permanent single-active-barcode invariant remained intact. No real customer/order/driver/Partner/legacy row changed. Independent read-only audit findings on missing live extension, caller-selected dates and fabricated operator role were closed. PR #44 remains open; exact paper scaling and camera decode remain the physical pilot. |
| 2026-07-29 | **WP-LBL-A27/A28 DRIVER/AREA BATCH LABELS DEPLOYED + REAL-SOURCE BROWSER PROOF** (Fix/Deployment/UAT Mode) | Added the supported `Nutrezee Batch Labels` page under the sole Fleet-Ops Resources menu. Operators can group by current-day Fleetbase driver or area, select all/subset, prepare one batch, open one print dialog, and separately confirm the physical result; permanent barcode issuance and reprints remain governed. The first authenticated live load exposed a timeout caused by requesting all 1,964 stored Fleetbase orders; corrected the API to use Fleetbase's supported server-selected `scheduled_at` filter plus bounded pagination and redeployed only API/Console with preserved rollback images. Authenticated proof as `MOHAMED MOSTAFA ALI GADELRAB` loaded `2026-07-29` successfully and showed the honest source total `0`, not the partial local `39`, with printing disabled until the complete current-day dispatch set exists. Full Vitest 69 files / 386 tests, focused 40/40, lint, API typecheck/build, public health/auth boundaries, cache headers, and unchanged database counts passed. No preview, print, Partner/legacy write, or real row change occurred. |
| 2026-08-08 | **WP-LBL-A29 PARTNER V2 LABEL SOURCE DEPLOYED + LIVE READ-ONLY PROOF** (Fix/Deployment Mode) | Closed the verified current-day empty-meal-table defect using the production Kitchen & Labels v2 contract. Commit `0147c86`; local 70 files / 393 tests; CI 31244633933 passed 14/14. The protected existing key was copied into the root-owned mode-0600 app secret file; no value appeared in source, commands, responses or logs. Deployed API image `d7108de4…` only, preserving `d6db7055…` as rollback. Compiled-image and running-service proofs returned `meal_source=partner_api_v2`, 4 complete rows for a mapped real order, and `nutrition_incomplete` for an incomplete real order. Pre/post counts remained customer 19,483; orders 20,204; print events 1; barcodes 1; collections 1. Public health 200 and unauthenticated label boundary 401 passed. No print, Partner/Fleetbase/legacy write, migration, customer/order mutation, or unattended dispatch activation occurred. Physical paper/camera proof and a complete current-day Fleetbase assignment feed remain operational dependencies. |
| 2026-08-08 | **WP-LOC-A30 CODE COMPLETE + CI GREEN / STAGING PROOF PENDING** (Build/Fix Mode) | Implemented the recurring 190-stop missing-location recovery without touching Navigator `/legacy` or Fleetbase vendor source. Partner-valid pins remain first; otherwise the manual bridge uses the latest append-only audited customer capture, then an explicitly labeled same-area active operational stop, then the area centroid. Navigator exposes only the authenticated driver's current assignments, navigates to fallback, calls that customer, previews current GPS/shared coordinates and submits with assignment/idempotency revalidation. Fleet-Ops gains an append-only reason-required correction page inside the supported extension. Commits `3664a32`, `3eace32`; draft PR #47 stacked on PR #44. Local Vitest 402/402, lint/typecheck/build/scans, PHP 26/26 and shell syntax passed; final push/PR CI runs 31252643564 and 31252645972 passed 14/14. Driver commit `afb1727` pushed to PR #1; local Jest 338/338 and targeted lint/bundle passed; GitHub run 31252463962 passed install/tests and the 25m12s Android build, producing the `android-debug-apk` artifact. No deployment, dispatch run, Partner/Fleetbase/legacy write, production activation, secret generation or paid-license purchase occurred. |
| 2026-08-08 | **WP-LOC-A30 STAGING SOFTWARE DEPLOYED + FLEET-OPS BROWSER PROOF** (Deployment/UAT Mode) | Verified root-only database/Fleet-Ops backups and rollback image tags, then deployed only additive migration `0029`, the A30 API image `ea51829a…`, the supported Console extension image `4b8dfaab…` and the checksum-matched manual bridge; protected migrations `0024`–`0026` remained absent. Public/local health, all four route registrations, unauthenticated `401` boundaries and cache-busted extension discovery passed. Authenticated Fleet-Ops proof loaded `Nutrezee Driver Locations / مواقع السائقين`, the append-only warning and correct empty state with no console error; no correction was submitted. The complete Partner dry-run walked 554 meal pages and 24 order pages, reconciled 3,210 meal rows / 739 orders, preserved 549 Partner pins, identified 172 same-area known-stop anchors, loaded 0 saved captures and completed with `fleetbase_written=false`. Pre/post business counts remained customer 19,483; orders 20,204; print events 1; barcodes 1; collections 1; location captures 0. The dispatch timer remained disabled/inactive and no capture export tempfile remained. Physical Navigator assignment/call/navigation/GPS-capture proof is still pending because ADB found no connected Android device. No Partner/legacy/Fleetbase write, live dispatch, production activation, secret generation, paid-license purchase, Navigator `/legacy` edit or Fleetbase vendor-source edit occurred. |
| 2026-08-08 | **WP-LOC-A30 NAVIGATOR EMULATOR ERROR-PATH PROOF + UI HARDENING** (Fix/UAT Mode) | Installed the exact green-CI A30 debug artifact from run `31252463962` on Android 14, cold-launched Navigator and reached Collect My Orders → Driver Locations in English and Arabic. The preserved authenticated identity was not a Fleetbase-verified driver, so staging failed closed without returning assignment/customer data. Emulator QA exposed and fixed a contradictory error-plus-empty-state render and missing explicit mappings for Fleetbase session/identity failures. Driver commit `e6c27b6`; focused A30 10/10 and full Jest 65 suites / 339 tests passed, with clean bilingual runtime rendering and no capture request. Physical proof still requires a real current driver plus one eligible missing/invalid-pin assignment to verify navigation, call, GPS/shared coordinates, confirmation, accepted/idempotent persistence and Fleet-Ops visibility. No Partner/legacy/Fleetbase write, dispatch, production activation, `/legacy` edit, secret generation or paid-license purchase occurred. |
| 2026-08-08 | **WP-LOC-A30 RELEASE/UAT GATE REVALIDATED — EXTERNAL INPUTS STILL ABSENT** (Review/UAT Mode) | Re-read both repositories and live registers, then verified current external state. Driver PR #1 and A30 PR #47 are clean/mergeable drafts; final source commit `e6c27b6` passed CI run `31257188765` (`install_and_test` + `android_build`). The exact debug artifact SHA-256 was `708e7bc0355f15382fefa19a7817f2cf6ebff96b2446bff3a3fc28af1cf0d46e`; its Android 14 install/cold launch produced no fatal or unhandled React Native error. Main documentation commit `5dbb1dc` passed push/PR CI runs `31257248945` + `31257251333` at 14/14 each. No ADB device is connected; the Transistorsoft license and Nutreeze signing/keystore inputs remain absent. Staging is healthy; health/auth boundaries passed, only migrations `0027`–`0029` are present in the protected range, A30 capture/audit counts remain zero, dispatch timer remains disabled/inactive and the read-only snapshot remains root-owned mode `0600` with `fleetbase_written=false`. No capture, correction, dispatch, production activation, Partner/legacy/Fleetbase write, secret generation or paid-license purchase occurred. Exact unblock remains a physical verified-driver handset plus an eligible assigned missing/invalid-pin order, followed separately by owner-supplied release license/signing inputs and explicit production approval. |
| 2026-08-08 | **WP-KDS-01 standalone build — IN PROGRESS** (Sprint Build Mode) | User-authorized A31 unit created directly from `origin/main`, replacing the earlier coupled/stacked approach. Added an independent `kitchen-display/` Node API + React/Vite UI + scrypt/opaque-session auth + dedicated Partner GET-only adapter + PII-free exact section/meal/portion aggregation + visible unrouted lane + read-only hardened container + separate six-job CI. Local separation scan/typecheck/build and npm audit (0 vulnerabilities) green; Vitest 5 files/25 tests, bilingual Chromium Playwright 1/1, and unchanged root baseline 46 files/204 tests green. PR #46 opened independently against main. Its first push run exposed that local compose treated the intentionally absent `.env` as required; changed it to an optional local env file and added explicit staging-compose validation. The corrected KDS push/PR workflows are 6/6 green (`31250240275`, `31250242382`) and the unchanged root push/PR workflows are 14/14 green (`31250240221`, `31250242377`). Added a separate credential-protected HTTPS Playwright configuration for live Arabic/English acceptance, including browser-contract arithmetic and prohibited-field checks; the local fixture journey and live-config discovery are green. No secret, deployment, Partner call, status transition, DB/legacy/upstream write, logistics/driver/label dependency, or main-system source change. Review/merge/deployment/live proof remain before DONE. |
| 2026-08-08 | **WP-KDS-01 PR review remediation — IN PROGRESS** (Fix Mode) | Thread-aware PR #46 review found 18 inline findings; two were already resolved by the frontier/Caddy corrections. Addressed all 16 remaining findings plus the four review-body nitpicks that were valid: read-only CI permissions and non-persistent checkout credentials; real isolated-container `/health` smoke; secret build-context exclusion; broadly compatible explicit Compose environment; Node 22.12 minimum and web typecheck in build; cumulative pagination bytes; bounded LRU-style login counters; rightmost validated proxy IP; graceful shutdown; six-decimal scaled quantities; browser timeout; stale-response suppression; language-toggle request stability; safe static caching; mobile sign-out; token-pattern reuse and close-event request logging. Unexpected exception messages/stacks remain deliberately excluded because they are not a proven secret-free surface. Local KDS boundary/typecheck/build and audit are green, Vitest increased to 6 files/31 tests, Playwright to 2/2 including out-of-order date regression and narrow-screen sign-out; unchanged root lint/typecheck/build, 46 files/204 tests and both scans are green. Review-fix push/CI and thread resolution remain. |
| 2026-08-08 | **WP-KDS-01 merge + safe deployment staging — BLOCKED on credentials** (Deployment Mode) | PR #46 review closed with 18/18 threads resolved and merged as `74a703c`; final pre-merge and post-merge KDS/root workflows are all green (`31251176353`, `31251176357`, `31251178414`, `31251178417`, `31251266861`, `31251266863`). Uploaded the exact merge artifact (SHA-256 `1e3b0f572a1c73b86538f20f457d64cc03064422db0b9c3220af6d2e9c2c500d`) into isolated `/opt/nutrezee-kds/repo`, created a mode-0700 dedicated secrets directory, validated staging Compose, and built hardened image `sha256:efe4a3ecbe416f2c9715190942e284ffcb902687ff115f05f4639e2469bb7289`. The combined existing-plus-KDS Caddy configuration validates. Confirmed zero KDS containers, zero port-8180 listeners, zero live KDS proxy matches, and both protected secret files absent/empty; therefore no service or proxy route was activated. Exact unblock: securely provision a dedicated KDS Partner read-only key and operator-selected display-password hash, then run health/HTTPS/Partner arithmetic/privacy/no-write/bilingual Playwright acceptance. |
| 2026-08-08 | **WP-KDS-01 completion audit + VPS runtime proof — credential blocker repeated** (Deployment/UAT Mode) | Re-ran the mandatory repository/VPS frontier checks: `main` is clean/current and both dedicated secret files remain absent/empty. Ran the exact staged image temporarily on the actual VPS with network disabled, read-only root, tmpfs, non-root `node`, no-new-privileges, and all capabilities dropped; `/health` returned the expected `nutrezee-kds` 200 payload, no Partner request was possible, and cleanup left zero containers/listeners. Confirmed the KDS hostname resolves to the VPS while TLS remains intentionally inactive because no live route exists. Added `docs/kds/02_completion_audit.md`, mapping every A-to-Z objective to authoritative evidence and keeping deployment/live arithmetic/privacy/no-write/bilingual acceptance explicitly open. The exact protected-input unblock is unchanged. |
| 2026-08-08 | **WP-KDS-01 production activation — DONE** (Deployment/UAT Mode) | User supplied the dedicated production Partner credential and explicitly directed autonomous production completion (A32). Discovered and fixed the non-root container/protected-file permission contract through supplemental group `61001`; PR #48 merged as `5955054` with KDS/root pre-merge CI 6/6 + 14/14 (`31252914322`, `31252914332`) and post-merge CI 6/6 + 14/14 (`31253038731`, `31253038740`). Installed exact artifact SHA-256 `57698350d3eff6602122a24963325043ad7e11be37d03998f4cdc9f42387e5fa`, image `sha256:dbff652868a8bf292b488ed367326b3003125cc29ce268bdffd6cb472f893a32`, protected secret files mode `0640` root:61001, and root-only initial-password handoff. Activated isolated Compose service plus KDS-only Caddy route at `https://kds.13-140-159-201.sslip.io`; container is healthy, restart count 0, non-root/read-only/cap-drop-all, and has no direct secret env. HTTPS/session/header/method/query checks passed. Independent GET-only verifier matched every live section/meal/portion projection across 3,178 rows (3,266 source quantity; 6,532 assignment quantity; six sections; 222 groups; zero unrouted) without retaining raw rows. API/browser/log privacy scans passed; formal protected live Playwright passed the applicable Arabic/English totals journey, and a 390px browser check showed no horizontal overflow. WP-KDS-01 promoted to DONE. |
| 2026-08-08 | **WP-KDS-01 English-default production follow-up — DONE** (Build/Deployment/UAT Mode) | User directed that the login page be English instead of Arabic and requested the active username/password (A33). PR #49 changed the initial HTML, login, and display to English/LTR, introduced a versioned language-preference key so browsers migrate once from the old Arabic-default value, retained the explicit persistent Arabic/RTL toggle, and updated unit/Playwright/docs coverage. Local boundary/typecheck/build, Vitest 31/31, fixture Playwright 2/2, and npm audit 0 passed. PR and post-merge KDS/root CI are green; final runs `31254271648` 6/6 and `31254271662` 14/14. Deployed exact release `3743aea` (artifact SHA-256 `afb83d997d45924554de724a43d10d7e8e5568edf97b62f4add01a094db25240`, image `sha256:505154f8ea522b178e8dd7cd4797c95a24668f6845099228873feaca904014c0`) with the previous release retained. Health/HTTPS are 200, restart count is 0, direct production browser shows `lang=en`, `dir=ltr`, English login fields and Arabic toggle, and protected live Playwright passed the applicable English-default/Arabic totals journey. |
| 2026-08-08 | **WP-KDS-01 operator-selected temporary password — DONE** (Deployment/UAT Mode) | User explicitly replaced the generated initial credential with a simpler temporary display password (A34). Generated a new random-salt scrypt hash through stdin, atomically installed it as mode `0640` root:61001, retained the prior hash only as a mode-`0600` root-only rollback file, updated the root-only plaintext handoff, removed the temporary input, and restarted only the KDS container. Health/HTTPS remained 200, container health is green with restart count 0, and a real English production-browser login plus logout succeeded with the new credential. The literal password is intentionally absent from Git and this register; the user was warned that it is weak and should remain temporary. |
| 2026-08-08 | **WP-KDS-02 user-assigned section isolation — DONE** (Sprint Build/Deployment/UAT Mode) | User rejected the all-sections board and required every section to see only its own meals/quantities based on the assigned user (A35). PR #50 merged the protected multi-user manifest, session-bound claims, server-only projection, assignment-scoped summaries, focused UI, and mutual isolation tests as `251e1f2`. Production activation proved all six code-named accounts and retained the previous release for rollback. The protected live Playwright gate then exposed a real 15-second browser vs 30-second server deadline mismatch on an uncached Partner read; PR #51 aligned the deadline and made the live verifier deterministic, merging as final release `0fd988a`. Final post-merge KDS/root CI is 6/6 + 14/14 (`31256752632`, `31256752652`). Exact artifact SHA-256 `86f596aaeaa15c235a7edc918adbedb7155c9141c27a6224984bb74fd4c93b80` and image `sha256:e30f357f0872d3ace10d36e1dc396b5a979ba3d93a17632add3dce76de0f095c` are active. Fresh-container protected live Chromium passed; direct production proof returned only `drinks` 35, `hot` 1,885, `pastry` 686, `salad` 618, `soup` 42, and `packing` 3,266; cross-section query returned 400. Runtime is healthy, restart 0, read-only/cap-drop-all, with zero error logs. |

| 2026-08-08 | **PR #43 synchronized with `main` — register histories reconciled** (Fix/Review Mode) | Merged the current `origin/main` KDS history into `build/partner-daily-fleetbase` without changing product behavior. Preserved the July driver/logistics amendments A17–A24 and assumptions ASM-051–054; renumbered the independently-added KDS records to A31–A35 and ASM-055–056 so every identifier remains unique. Local root typecheck/lint/build, 62 files / 313 tests, boundary/no-GET scans, and snapshot guards 6/6 passed; KDS boundary/typecheck/build, 6 files / 34 tests, fixture Playwright 3/3, and npm audit 0 passed. Docker is unavailable locally, so the existing CI container jobs remain the image/Compose gate. No deployment, production/Partner/Fleetbase/legacy write, credential mutation, order reassignment, or dispatch activation occurred. Push CI `31267258889` and PR CI `31267261571` both passed 14/14, including Docker/Compose validation; PR #43 is clean and mergeable. |

| 2026-08-08 | **PR #44 synchronized with its operational parent — register histories reconciled** (Fix/Review Mode) | Merged `origin/build/partner-daily-fleetbase` into `build/wp-lbl-a27-legacy-label-barcode` after PR #43's main/KDS synchronization. Preserved label/driver amendments A25–A29 and KDS amendments A31–A35 alongside the unchanged July A17–A24 history; the next canonical id remains A36 because A30 is already allocated on the active child PR #47. Local root typecheck/lint/build, 70 files / 393 tests, boundary/no-GET scans, and snapshot guards 6/6 passed; KDS boundary/typecheck/build, 6 files / 34 tests, fixture Playwright 3/3, and npm audit 0 passed. No merge to base, deployment, print, collection scan, production/Partner/Fleetbase/legacy write, credential mutation, reassignment, or dispatch activation occurred. Push CI `31267484996` and PR CI `31267486874` both passed 14/14, including Docker/Compose validation; PR #44 is clean and mergeable. |

| 2026-08-08 | **PR #47 synchronized with its label parent — full stack history reconciled** (Fix/Review Mode) | Merged `origin/build/wp-lbl-a27-legacy-label-barcode` into `build/wp-ops-a30-driver-location`, preserving A30 missing-location recovery alongside label/driver A25–A29, KDS A31–A35, and the July A17–A24 history. Local root typecheck/lint/build, 72 files / 402 tests, boundary/no-GET scans, and snapshot guards 6/6 passed; KDS boundary/typecheck/build, 6 files / 34 tests, fixture Playwright 3/3, and npm audit 0 passed. No merge to base, physical-driver capture, correction, deployment, production activation, print, collection scan, production/Partner/Fleetbase/legacy write, credential mutation, reassignment, or dispatch activation occurred. Push CI `31267670958` and PR CI `31267673371` both passed 14/14, including Docker/Compose validation; PR #47 is clean and mergeable. |

| 2026-08-12 | **WP-OPS-01 A36 authoritative daily selector repair + exact Aug-12 Fleetbase sync** (Fix/Deployment Mode) | Verified Partner's new live `/integration/daily-deliveries` contract and replaced the incomplete meal-history/orders selector. The bridge reconciles cursor rows to Partner's declared deliveries and distinct orders, uses unique `order_id`, preserves duplicate delivery instances in the digest, and rejects ambiguous groups or old-selector date prefixes. PHP 32/32, snapshot 8/8, local typecheck/lint/build + 402/402, and push/PR CI runs `31625967961` + `31625971480` at 14/14 passed. Deployed checksum-matched scripts with a root-only rollback set. Two Aug-12 reads matched 891 rows / 883 orders and digest; the guarded manual write committed 883 unique Fleetbase orders, 671 dispatched/assigned and 212 safely held (206 missing, 6 invalid pin), plus 883 payloads/tracking and zero duplicate ids. The controlled schema-v2 snapshot stored the same 883 without PII and with `fleetbase_written=false`. Aug-13 two-pass preflight matched 874 rows / 856 orders and performed no write. Fleetbase is healthy; snapshot timer enabled/active; dispatch timer disabled/inactive; Partner/legacy remained read-only and A30 production activation was not enabled. |
| 2026-08-12 | **WP-OPS-01 A37 exact Aug-13 sync + rolling 48-hour production activation** (Fix/Deployment Mode) | Direct Driver Orders export proved 855 unique Aug-13 orders and exact set comparison found one API-only row. Added a root-protected PII-free membership manifest with strict date/count/digest/API-superset validation, plus explicit zero-date normalization for the API's omitted `per_date` entry. Two independent reads matched 855 and the guarded write created exactly 855 Fleetbase orders/payloads/tracking records: 657 dispatched/assigned, 198 held, 0 duplicates, and the API-only row absent. Aug-14 matched 0 twice and passed the explicit zero-day verification. Activated the 07:00 Kuwait rolling +1/+2-day timer; dates run independently, the service is `Restart=no`, and the prior read-only 01:00 snapshot stays separate. PHP 35/35, rolling sync 7/7 and snapshot 8/8 passed; exact deployment hashes and rollback copies were verified. |
| 2026-08-12 | **WP-OPS-01 A38 rolling timer corrected to 01:00 Kuwait** (Fix/Deployment Mode) | Mohamed corrected the production schedule from 07:00 to 01:00 Kuwait. Changed the systemd calendar to 22:00 UTC on the preceding day and aligned the executable start guard to 00:45–01:45 Kuwait. Restarted only the timer, not the dispatch service, so this correction did not repeat or mutate the already-complete Aug-12/Aug-13/Aug-14 data. Rolling-sync shell tests passed 9/9, including rejection of a 07:00 start before any runner call; deployed timer/script hashes and the next Kuwait trigger were verified. |
| 2026-08-16 | **WP-OPS-01 A39 nullable Partner time-slot repair + exact Aug-18 recovery** (Fix/Deployment Mode) | The 01:00 rolling service completed Aug-17 (847 total; 639 dispatched/assigned, 208 held) but failed closed on Aug-18 because one otherwise valid Partner row returned a legacy `time_slot` object with all four values null. Updated only the contract boundary so null optional time-slot presentation values are accepted while non-null wrong types still fail; protected pickup `dispatch_time`, membership, count/digest, location holds, and idempotency are unchanged. PHP syntax/self-test passed 37/37, snapshot guards 8/8, and rolling-sync guards 9/9. Deployed script SHA-256 `84a43574cba3360b449e1bbdfcfb58de43f37325db8586d7a62a9632aaeb93cb` with a root-only mode-0600 rollback. Two Aug-18 reads matched 835 and digest; the guarded write produced exactly 835 Fleetbase orders/payloads/tracking records: 628 dispatched/assigned, 207 safely held (201 missing, 6 invalid pin), and 0 duplicate ids. The failed state was reset without starting another run; the service is inactive/success with `Restart=no`, and the timer remains enabled/waiting for 01:00 Kuwait. Partner/legacy remained read-only and A30 capture activation remained separate. |
| 2026-08-16 | **WP-LBL A40 driver color + vehicle/phone box identity implemented; production data blocker verified** (Build/Review Mode) | Added a deterministic 16-color driver palette keyed by immutable Fleetbase public id across the complete company directory, live server-side phone/vehicle enrichment, and fail-closed validation for every assigned label. Individual and batch 100 × 70 mm labels now show a prominent bilingual CAR/TEL color band, never a mutable driver name; the permanent customer Code 128 bars/value remain black and unchanged. Reassignment is resolved from Fleetbase again on preview. No label/audit persistence, Partner/Fleetbase write, deployment or live print occurred. Current production inspection found phone values for all 11 active drivers but 0 vehicles, 0 plates and 0 driver-vehicle assignments, so rollout is deliberately blocked until operations enters real data. Local full Vitest passed 72 files / 405 tests; focused A40 45/45, typecheck, lint, build, boundary/no-GET scans and snapshot guards 8/8 passed. Commit `52bfaa8`; push + PR CI `31955378733` / `31955380616` both passed 14/14. |
| 2026-08-18 | **WP-LBL A41 nine supplied vehicles linked to active Fleetbase drivers** (Operations Mode) | Mohamed authorized the immediate temporary phone-matched linkage to finish the label project. The supplied `Nutrezee_Driver.xlsx` contains nine real plate values and two blank plate cells. Each real plate was matched server-side to exactly one current Fleetbase driver using the workbook name plus unique phone suffix, then created and assigned through Fleetbase's supported authenticated `POST /v1/vehicles` API. Post-write reconciliation proved 9 company vehicles, 9 unique nonblank plates, 9 distinct active-driver links, 0 duplicate-plate groups and 0 cross-company link errors. Phone was never substituted for a plate or persisted in vehicle fields. Ahmed Al-Salem and Unit-01 remain unlinked because no plate was supplied; Demo Driver - Salmiya is outside the 11-driver active roster and remains unlinked. No Partner/legacy write, secret generation/exposure, vendor-source edit, label deployment or print occurred. |
| 2026-08-21 | **WP-OPS-01 A42 unstarted-job reconciliation repair + exact Aug-22 recovery** (Fix/Deployment Mode) | The 01:00 rolling service correctly isolated Aug-23 but failed Aug-22 because 123 existing Partner rows advanced `ordered` → `driver_assigned`, three received timestamp-only changes, and ten unstarted rows disappeared. Both statuses were already approved, all affected Fleetbase jobs were unstarted, and the arithmetic reconciled exactly from 684 to 674. Replaced the raw-hash freeze at pre-dispatch with an explicit `started`/`started_at` freeze helper; unstarted integration-owned rows now flow to the existing atomic writer, while started source/routability/driver changes and missing allocation still fail closed. PHP syntax/self-test passed 38/38 in the Fleetbase container; snapshot 8/8, rolling sync 9/9, full Vitest 72 files / 405 tests, lint/typecheck/build, and push/PR CI `32476560894` / `32476563957` at 14/14 all passed. Deployed only `nutreeze-orders.php` SHA-256 `405d2e507333ec6489c0d8551eb08cb8c0beed00d3ee1bc9d09ea4090d7e4a8e`, retaining the previous `84a435…` file as root-only mode-0600 rollback. Stable source reads matched 674 and digest `3b8179…`; the guarded write committed 500 dispatched/assigned and 174 current held, retained the ten missing rows as canceled/unassigned/unscheduled audit tombstones, and verified 684 unique order/payload/tracking identities with 0 started rows. Aug-23 remains 790 (582 assigned, 208 held). The failed state was reset without another service run; service is inactive/success with `Restart=no`, timer enabled/active for 01:00 Kuwait. Partner/legacy stayed read-only; A30 activation remained separate. |

## Amendments log (continues `phase_4_to_build_handoff.md` §4 — A1–A4 recorded there)

| ID | Date | Amendment | Raised by |
|---|---|---|---|
| A5 | 2026-06-10 | WP-00 "Environment standup" added ahead of WP-01 to satisfy global gate ④ (staging+CI) — resolves the bootstrap circularity in the Phase 4 gate; entry gate ①+③ only, no business code; staging provisioning conditional on a PG-region residency note in 20_Decisions/ (stop-rule carve-out); CI guards remain placeholders until WP-01 implements them | Phase 5 master prompt |
| A6 | 2026-06-10 | `ingredient_allergen` link table added (wave-2 catalog DDL) — AllergenResolver's derived_from_ingredient semantics required ingredient→allergen links missing from the logical data model; structure-only, discovered during WP-05 | WP-05 build session |
| A7 | 2026-06-10 | Assumption-carry control added for WP-07+ after sponsor/user directive: unresolved business decisions are captured in `ASSUMPTION_REGISTER.md`, assigned risk, marked sponsor-review-required, and allowed for implementation until revised; hard technical gates, dormant-module boundaries, real legacy access/export, and WP-14 staging gate still stop the build | Sponsor/user directive |
| A8 | 2026-06-10 | `draft_order.unverified_reason` added in WP-07 to carry the explicit unverified-customer submit justification required by ASM-005/006 without creating a separate review table before WP-08; structure-only, intake-owned, reversible if sponsor signs a different identity workflow | WP-07 build session |
| A9 | 2026-06-10 | WP-08 added a boundary-safe M02→M01 review port and `TransitionEngine.transitionInTx` so review decisions can atomically return/reject drafts without M02 writing `draft_order`; approve records an append-only review decision and emits `order.approved` with `conversion_pending_wp09` because OrderFactory belongs to WP-09 | WP-08 build session |
| A10 | 2026-06-10 | WP-09 added the M03 wave-4 order core tables and service ports: M03 owns `customer_order`, `order_item`, `fulfillment_day`, `order_status_history`, `change_request`, and `exception_case`; M03 reads approved review/customer/catalog/draft data only through owning-module APIs, then converts drafts via the M01 transition port. Added assumptions ASM-043..046 for continuous day generation, unverified-customer conversion block, exception taxonomy, and end-date-only change requests. Also fixed M03 date serialization to preserve PostgreSQL `date` calendar values under Asia/Kuwait timezone. | WP-09 build session |
| A11 | 2026-06-10 | WP-10 added the M08 wave-5 kitchen tables and service ports: M08 owns `kitchen_ticket`, `ticket_status_event`, `section_master`, and `escalation`; M08 reads order days/items through M03 and product routing through M05, generates idempotent section/unrouted tickets, records shared-device ticket status events, rolls fulfillment days through the status engine, supports pack confirmation, and raises/resolves kitchen escalations. Added assumptions ASM-047..048 for product-level routing scope and pack-confirmation override posture. | WP-10 build session |
| A12 | 2026-06-10 | WP-11 added the M07 wave-4 payment-lite tables and service ports: M07 owns `payment_record` and `payment_review_item`, uses the config-seeded payment machine for status changes, records manual link-sent events, queues finance payment reviews with evidence, keeps paid/failed/collected decisions FI-only, masks non-finance payment reads, exposes refund endpoints only as `not_enabled`, and provides a legacy-unmapped status queue port for WP-13. Added assumption ASM-049 for payment record creation timing and non-mutating payment reads. | WP-11 build session |
| A13 | 2026-06-10 | WP-12 added the M11/M15 notifications-and-reports slice: M11 owns `notification_template` and append-only `notification_log`, logs internal/email-capable notifications from configurable trigger-map rules, dedupes by source event/template/recipient, and keeps customer/WhatsApp/push/SMS notifications dormant; M15 owns no business tables and rebuilds intake funnel, daily ops, and kitchen day-list projections directly from `outbox_event`, with audited JSON exports and TS-E replay-equality coverage. | WP-12 build session |
| A14 | 2026-06-10 | WP-13 added the M18/M19 bridge-and-cutover slice: M18 owns append-only `reconciliation_run`, records reconciliation diffs with WARN audit/outbox on divergence, and wraps platform cutover-flag updates with HIGH audit; M19 adds active-plan batch-3 import through M03/M07 owner ports, synthetic dry-run/apply/rollback, persisted import reports, API endpoints, unmapped legacy payment statuses to finance review, and unverified off-day/address markers under ASM-036..038/050. Real legacy apply remains blocked on legacy export/access. | WP-13 build session |
| A16 | 2026-06-12 | **WP-UI added** (sponsor-directed in-session after seeing staging live): admin SPA screens over the existing API, staged UI-01 (login + shell + sidebar + kitchen + read-only lists) then UI-02+ (the WP-14 UAT minimum screen set). Resolves the plan's internal gap: Phase-6 UAT requires per-persona browser journeys (WF-01…08, 12–16) but WP-01–13 scoped only login-shell/role-UI-API/message-ref-panel/kitchen-board; the WP-01 login shell itself was never actually built (smoke logged in via curl). UI-only — no API changes authorized under this row. | Sponsor (chat, 2026-06-12) |
| A15 | 2026-06-11 | Independent-review corrections (see `wp01_wp13_independent_review.md`): migration `0013` adds the schema-mandated `payment_record(order_id)` index and removes the structurally-dead `dormant_role_granted` notification trigger (security events are audit-only, never on the outbox — the dormant-grant alert stays the WP-02 HIGH audit event, partially revising ASM-034's trigger list); M02 gains the missing `review.sla_alert` emitter the WP-12 trigger map already routed; M04/M05 gain M19 owner-rollback ports so import rollback no longer writes foreign tables; M19 apply/rollback now emit `bridge.import_run` to the outbox per module spec. | Review session |
| A17 | 2026-07-18 | Mohamed authorized modifying Navigator app source (`/src`) for the driver-authentication feature only: login screens, auth service calls, secure credential storage, and biometric unlock. AGPL-3.0 compliance requires modified source be provided to Nutreeze staff app users on request; the fork remains in the private repo. All other `/src` areas and all `/legacy` remain off-limits; Fleetbase vendor/backend source stays config/API/admin-console only. This explicitly lifts the dormant driver-app boundary only for the named authentication slice. | Sponsor/user directive |
| A18 | 2026-07-19 | Mohamed authorized the operational Partner API → Fleetbase → Navigator daily-delivery bridge end to end for the 11 existing drivers. The bridge may read the Partner API, create/update only its own date-scoped Fleetbase records, assign routable deliveries, retain the current API key in a protected mode-0600 VPS file until Mohamed rotates it, and capture API/app proof. Partner/legacy writes, `/legacy`, Fleetbase vendor source, driver phone changes, and the unit/SIM redesign remain prohibited. Rows without an exact source pin must stay held and unassigned. | Sponsor/user directive |
| A19 | 2026-07-20 | Mohamed authorized a one-day exception for delivery date 2026-07-20 only: otherwise-approved rows without a valid customer pin may be assigned by known textual routing area when source address and customer phone are present, with a visible instruction that the driver must call the customer for the exact location. The area centroid remains explicitly labeled as fallback, unknown-area country fallbacks remain held, the exception requires an exact manual date confirmation, and it is forbidden from the unattended timer or any other date. All other A18 boundaries remain unchanged. | Sponsor/user directive |
| A20 | 2026-07-20 | Corrected the daily Partner meal contract after the live July 20 preflight exposed repeated `meal_id` values. The supplied Partner documentation defines `/integration/meal-history` as one row per meal item per delivery date and defines `meal_id` as a meal-catalog reference, not a unique row key. The bridge therefore aggregates every validated meal row (including repeated catalog IDs) into the delivery instead of rejecting a legitimate repeated item; customer/status conflicts and unstable count/digest still fail closed. | WP-OPS-01 implementation finding, verified against supplied Partner API PDF |
| A21 | 2026-07-22 | Mohamed authorized a narrow driver-credential operation for the Logistics Manager in the Nutrezee admin panel. Scope: list the existing Fleetbase driver accounts and rotate only a selected driver's password through Fleetbase's standard password-change service, with Nutrezee RBAC and HIGH audit evidence. The server must derive the target user from a Fleetbase driver record; it must never accept an arbitrary user UUID, expose a privileged Fleetbase token, store/log/email the password, grant the manager Fleetbase's unrestricted `iam change-password-for user` permission, modify Fleetbase vendor source, or touch Navigator `/legacy`. All other deferred driver/admin scope remains prohibited. | Sponsor/user directive |
| A22 | 2026-07-22 | Mohamed authorized the Logistics Manager to hold the complete current Nutrezee logistics permission set and reassign any selected number of Fleetbase orders from one driver to another. The operation must use standard Fleetbase APIs, accept only public driver/order ids in the browser, derive upstream UUIDs server-side, process without a product batch-size cap, record HIGH batch/per-order outcomes, expose no customer/payload PII, and leave unrelated finance/staff/RBAC/catalog/system permissions, Fleetbase vendor source, and Navigator `/legacy` untouched. No live reassignment is authorized merely for build verification. | Sponsor/user directive |
| A23 | 2026-07-23 | Mohamed authorized an unattended daily read-only Partner API snapshot after the documented 06:00 source publication. It may perform two complete date-scoped reads and retain only root-protected, PII-free aggregate evidence with count, digest, hold/location totals, capture time, and an explicit non-authoritative completeness label. Partner/legacy and Fleetbase writes, raw-row retention, dispatch-timer enablement, and reuse of A19 remain prohibited. | Sponsor/user directive |
| A24 | 2026-07-23 | Mohamed changed the unattended read-only Partner snapshot schedule from 06:30 to **01:00 Kuwait** (`22:00 UTC` on the preceding calendar day). This supersedes A23's after-06:00 timing even though it precedes the previously documented 06:00 source publication; completeness must remain explicitly non-authoritative. All A23 write, PII, retention, dispatch-timer, and A19 boundaries remain in force. | Sponsor/user directive |
| A25 | 2026-07-26 | Mohamed explicitly instructed Codex to proceed without waiting and complete the Nutreeze driver app for distribution. The smallest necessary Navigator `/src` changes are authorized for the verified A-to-Z defects: KWD-only fuel reports and amount/status persistence; null-safe order documents; post-completion Orders state/navigation; chat participant/channel persistence; customer call/navigation behavior; Arabic language availability; and background-tracking integration fixes that do not require a commercial license. Each source file must be predeclared and regression-tested. `/legacy`, Fleetbase vendor/backend source, production/Partner writes, secrets, and paid-license purchase remain prohibited. | Sponsor/user directive |
| A26 | 2026-07-26 | Mohamed reaffirmed that the driver app must be complete end to end and that every reachable screen and visible action must be tested. This extends A25 to the minimum Navigator `/src` repairs for defects confirmed by the exhaustive reachable-screen, button, null-fixture, and route-contract audit recorded in the driver repo `PLAN.md`. Every source file remains predeclared and regression-tested; `/legacy`, Fleetbase vendor/backend source, production/Partner writes, secret generation, and paid-license purchase remain prohibited. | Sponsor/user directive |
| A27 | 2026-07-27 | Mohamed authorized reproduction of the existing legacy daily-meal label with the same structure, fields, field order and printing layout, plus **one permanent Code128 barcode per customer**. Scope: authoritative meal/nutrition sourcing (no fabricated values); governed `customer_barcode` records with idempotent issuance, merge-alias preservation and audited administrative replacement; individual and batch label printing with audit-controlled reprint reasons; current-day driver-manifest validation; collection scanning with same-transaction audit; and the minimum predeclared Navigator `/src` changes required for the collection screen and `Code 128` scanning. The barcode encodes no PII. Partner and legacy systems remain read-only. Fleetbase vendor source, Navigator `/legacy`, secret generation, commercial licence purchase and unattended dispatch activation remain prohibited. | Sponsor/user directive |
| A28 | 2026-07-28 | After reviewing the deployed A27 result, Mohamed explicitly authorized the corrective implementation: keep `ops.nutreeze.com` as the single Fleetbase/Fleet-Ops operations admin; remove the live `/nz-admin` SPA mount and `/labels` redirect while preserving `/nz/*` and all Fleet-Ops routes; remove the corresponding dual-admin source/image changes; add label/barcode/collection operations through a supported custom Fleetbase Console extension with only documented dependency/registration wiring; and use Fleetbase identity plus Fleetbase-assigned orders as the sole driver/assignment authority. The second Nutrezee driver login and synthetic Nutrezee route authorization path are retired. Drivers may see name/area/phone only for their own assigned work. The permanent barcode, audit/append-only controls, no-fabrication rule, Partner/legacy read-only boundary, production-write prohibition, Navigator `/legacy` prohibition, secret-generation prohibition, paid-license prohibition and unattended-dispatch prohibition remain in force. | Sponsor/user directive |
| A29 | 2026-08-08 | Mohamed instructed Codex to close the verified label-data gap immediately after the production Partner key and Kitchen & Labels v2 documentation were provided. The label service may perform server-side read-only GETs to `/integration/order-items` and `/integration/meal-catalog-v2`, join only by documented `meal_id`, and select an order only by exact delivery date and order number. The key remains server-held and must never be returned, logged, or committed. Missing/incomplete upstream meal nutrition blocks printing explicitly; no name matching, inference, fabrication, or Partner writes are allowed. Existing authoritative local dish-day records remain valid for historical/manual-entry operation. Fleetbase remains the current-day order/assignment authority; all A28 prohibitions remain in force. | Sponsor/user directive |
| A30 | 2026-08-08 | Mohamed authorized the recurring assigned-driver missing-location workflow for Navigator and the sole Fleet-Ops admin. A current assigned order with no valid Partner pin may route to the nearest same-area known operational anchor, explicitly marked as fallback; the driver calls the customer and captures exact GPS or a customer-shared coordinates/maps link. Fleetbase identity/current assignment is the only driver authority. Captures are append-only/audited and keyed by stable Partner customer reference; they may fill future missing/invalid rows, while a valid Partner pin always wins. Fleet-Ops governs audited corrections. Anchor-customer identity is never exposed. Partner/legacy remain read-only; staging implementation/proof only until explicit production activation approval. No unattended dispatch, vendor source, Navigator `/legacy`, secret generation or paid-license purchase. | Sponsor/user directive |
| A31 | 2026-08-08 | User explicitly authorized finishing a Kitchen Display subproject from A to Z and clarified it is entirely separate from the car/driver application. WP-KDS-01 is therefore a standalone top-level service based directly on `main`, with its own API/UI/auth/package lock/container/CI/deployment boundary and dedicated Partner read-only credential. Release 1 is totals-only by dynamic upstream section + exact `meal_id` + `portion_size`, includes every multi-section assignment and a visible unrouted lane, and never projects PII/physical/order identifiers or performs Partner/database/workflow writes. Runtime dependencies are limited to React/ReactDOM; TypeScript/Vite/Vitest/Playwright are build/test-only and recorded because the unit requires a typed bilingual UI plus visible acceptance evidence. | Sponsor/user directive |
| A32 | 2026-08-08 | User supplied the dedicated KDS production Partner credential, explicitly directed use of the Nutrezee VPS, and instructed autonomous completion to production without another credential stop. This authorizes protected server-side installation of that credential, server-side generation of an initial display credential with a root-only handoff, activation of only the standalone KDS Compose service and hostname, and read-only live Partner/HTTPS/browser acceptance. The credential contents remain forbidden from source control, documentation, ordinary chat, browser contracts, and logs; all A31 separation/no-write/privacy boundaries remain in force. | Sponsor/user directive |
| A33 | 2026-08-08 | User requires the Kitchen Display login and initial display to open in English rather than Arabic and explicitly requests the active username/password handoff. English/LTR is therefore the fresh-device and one-time migrated default; Arabic/RTL remains an explicit operator toggle whose new preference persists. This changes presentation only and does not alter A31/A32 read-only, privacy, section-total, credential-storage, or standalone boundaries. | Sponsor/user directive |
| A34 | 2026-08-08 | User explicitly selected a simpler temporary KDS display password despite the security trade-off. The literal value is not recorded in Git. Its scrypt-hash-at-rest, protected-file, login-throttle, opaque-session, HTTPS, and standalone boundaries remain mandatory; operations should rotate it promptly to a strong credential. | Sponsor/user directive |
| A35 | 2026-08-08 | User rejected the shared all-sections Kitchen Display and explicitly required every section to see its own meals and total quantities separately based on the assigned user. WP-KDS-02 therefore replaces the single credential with protected configurable users, binds exact section-code assignments to the server-side opaque session, returns only assigned-section totals and scoped summary metadata, removes any manual section-selection path, and proves mutual isolation at API and browser boundaries. The standalone/read-only/no-PII/no-driver/no-write boundaries remain mandatory. | Sponsor/user directive |
| A36 | 2026-08-12 | After Partner exposed the exact-date `/integration/daily-deliveries` endpoint and the user directed immediate permanent repair, WP-OPS-01 replaces the incomplete meal-history/orders daily selector with that endpoint. The bridge must reconcile the full cursor walk to Partner's declared delivery and distinct-order totals, create one Fleetbase job per unique `order_id`, preserve all delivery-instance ids/states in its digest, and fail closed on ambiguous duplicates or a selector/version mismatch with an existing date prefix. Verified contracts: 2026-08-12 is 891 delivery rows / 883 distinct orders; 2026-08-13 is 874 / 856. Partner remains read-only and the unattended dispatch timer remains disabled pending separate activation approval. | Sponsor/user directive + verified Partner contract |
| A37 | 2026-08-12 | Mohamed directed immediate Fleetbase availability through 2026-08-14 and recurring 48-hour-ahead operation for drivers. This explicitly authorizes the daily dispatch timer and supersedes A36's activation hold. The timer refreshes Kuwait tomorrow and the following day at 07:00, isolates each date, performs guarded count/digest reconciliation, and must not retry a failed service. When Partner membership exceeds Driver Orders, a root-protected PII-free exact order-number manifest may narrow the API only if every operational order is present; no hard-coded exclusion is permitted. Verified result: Aug-13 855 exact orders (657 dispatched, 198 held, 0 duplicates); Aug-14 0. Partner/legacy remain read-only and A30 capture production activation remains separate. | Sponsor/user directive + verified Driver Orders/API/Fleetbase reconciliation |
| A38 | 2026-08-12 | Mohamed corrected the recurring rolling Fleetbase synchronization time: it must run at **01:00 Kuwait**, not 07:00. This supersedes only A37's schedule. The systemd calendar is 22:00 UTC on the preceding day and the wrapper rejects starts outside 00:45–01:45 Kuwait. The +1/+2-day horizon, independent per-date reconciliation, exact membership manifest, `Restart=no`, Partner/legacy read-only boundary, and separate A30 activation gate remain unchanged. The correction must restart only the timer and must not trigger an additional dispatch run. | Sponsor/user correction |
| A39 | 2026-08-16 | Mohamed directed immediate live repair after Partner's Aug-18 daily-delivery source returned a legitimate row with a present legacy `time_slot` object whose `id`, `title`, `start`, and `end` were all null. Those fields are optional presentation metadata and their absence must not block an otherwise valid delivery; non-null values remain strictly type/length checked. Fleetbase scheduling remains governed only by the protected pickup `dispatch_time`, and all exact membership, count/digest, location-hold, idempotency, Partner/legacy read-only, `Restart=no`, and separate A30 activation controls remain unchanged. | Sponsor/user directive + verified Partner contract |
| A40 | 2026-08-16 | Mohamed requires each current Fleetbase-assigned driver's boxes to carry a distinct color plus the current vehicle number and phone because display names may change. Color derives only from immutable driver public id across the complete current company directory; vehicle/phone are read live server-side and assigned rows fail closed if any identity field is absent. The customer Code 128 remains black/unchanged. This narrowly supersedes the earlier driver-phone prohibition for authenticated internal Fleet-Ops label operations only and authorizes no storage/logging, Partner/Fleetbase write, public exposure or production deployment without real vehicle assignments. | Sponsor/user directive |
| A41 | 2026-08-18 | Mohamed explicitly authorized immediate Fleetbase vehicle linkage using the supplied driver workbook and phone as a temporary matching key. Scope is one-time operational creation and assignment of only the nine vehicles with real plate values, through Fleetbase's supported API, after each workbook row resolves to exactly one current driver. Phone must not be copied into any plate/vehicle field, no value may be inferred for blank plate cells, and missing plates continue to block those assigned drivers and A40 deployment. Partner/legacy writes, Fleetbase vendor-source edits, secret generation/exposure and unattended label activation remain prohibited. | Sponsor/user directive |
| A42 | 2026-08-21 | Mohamed authorized immediate repair of the failed production rolling sync after the +1-day Partner snapshot changed from 684 to 674 distinct orders: 123 existing rows advanced from `ordered` to `driver_assigned`, three received timestamp-only updates, and ten unstarted rows disappeared. Both lifecycle statuses are already dispatch-approved. A stable count/digest-locked source may atomically refresh, reassign, hold, cancel or tombstone an integration-owned Fleetbase job until Navigator records `started`/`started_at`; started jobs remain immutable and fail closed on source, routability or driver changes. Raw hashes remain provenance rather than a freeze boundary for an unstarted pre-dispatched job. All Partner/legacy read-only, exact reconciliation, transaction, `Restart=no`, vendor-source, secret, paid-license and separate A30 activation boundaries remain. | Sponsor/user directive + verified live reconciliation |
| *next: A43* | | | |
