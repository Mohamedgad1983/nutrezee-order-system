# Build Progress Register

**Status:** Living document — updated by every Phase 5 build session (see `phase_5_master_prompt.md` STEP 5).
Created 2026-06-10. WP definitions: `codex_implementation_sequence.md` (WP-01…14) and `phase_5_master_prompt.md` (WP-00, amendment A5).
**This file's §Amendments table is the sole canonical A-id counter** — stale "next free id" notes in earlier documents are superseded.
*(Date note: Phase 2–4 documents carry 2026-06-11 date stamps written one day ahead in error; git history — all commits 2026-06-10 — is authoritative.)*

## Gate snapshot (re-verify live each session — last verified 2026-06-11)

| Gate | State |
|---|---|
| ① DEC-011 stack signed | ✅ SIGNED 2026-06-10 |
| ② DEC-003 MVP cut signed | ✅ **SIGNED 2026-06-10** (`20_Decisions/DEC-003_mvp_cut.md`) |
| ③ R1 remote backup | ✅ CLOSED 2026-06-10 (GitHub, both branches) |
| ④ Staging + CI live | ✅ **BOTH HALVES DONE 2026-06-12.** CI half: green since WP-00; **14 jobs** incl. `docker-validate` (PR #2 merge `9a86ff4`, 2026-06-11). Staging half: ✅ **LIVE** — sponsor provided a VPS (`20_Decisions/NOTE_vps_staging_host.md`); deployed `main` `d69a107` + D7 nginx fix behind Caddy TLS at `https://13-140-159-201.sslip.io`; 13/13 migrations, super admin bootstrapped, **10/10 smoke passed** (checklist §6 evidence). Remaining WP-14 entry item from this gate: restore drill (nightly dumps in place, drill not yet run) |
| ⑤ Workshop held / assumption-carry acceptance | ✅ **Assumption-carry recorded 2026-06-10 for WP-07+** (`20_Decisions/NOTE_assumption_carry_wp07_plus.md`, `ASSUMPTION_REGISTER.md`); workshop itself still outstanding and all assumptions stay sponsor-review-required |

**Current frontier: WP-UI-02** (see `19_Roadmap/NEXT_ACTION_QUEUE.md`). WP-00–13 complete; WP-UI-01 DONE & merged (PR #3, `b06d646`); **WP-API-01 DONE & merged** (PR #4, `f9dcae6`) + D8 nginx-proxy fix (`0c3af5a`), deployed + verified on staging. Engineering path to legacy-order-ops replacement: ~~WP-API-01~~ → **WP-UI-02** → WP-UI-03 → WP-UI-04 (`Legacy_Core_Gap_To_Cutover.md`). **WP-14 remains gated** on the sponsor-owned parallel track (legacy export access + workshop pack) — not on engineering. WP-07+ unresolved business questions remain sponsor-review-required in `ASSUMPTION_REGISTER.md`. Agents continue autonomously via **"Continue Nutrezee OS Agent"** (`AGENTS.md` → `AUTO_EXECUTION_RULES.md`).

## Work package status

| WP | Title | Status | Branch / merge commit | Suites green | Amendments | Notes / NC carried |
|---|---|---|---|---|---|---|
| WP-00 | Environment standup | **DONE 2026-06-10** | `build/wp-00-environment-standup` → merge `e704eca` (feat `213478c`) | lint ✅ typecheck ✅ build ✅ placeholder suites 8/8 ✅ + `GET /health` runtime smoke ✅ (all local) | A5 | Staging deferred per A5 carve-out (no PG-region note); compose authored but unvalidated locally (Docker not installed); first GitHub CI run to be verified post-push |
| WP-01 | Platform foundation | **DONE 2026-06-10** | `build/wp-01-platform-foundation` → merge `569dfe1` (feat `fddb1d1`) | Local: 25/25 (TS-I, TS-A #2/#3, TS-R generated 132-case, TS-U auth) + lint/typecheck/build/scans. CI run 27281542344: **13/13 jobs green** incl. DB-backed suites vs postgres:16 service + real boundary/no-GET-mutation scans | — | Wave-1 schema (18 tables) + seeds (12 roles, 48 transition rows [Proposed DEC-005], 13 settings); audit/outbox/idempotency/sessions/staged-RBAC platform. NC carried: DEC-005 enums Proposed; argon2 params pinned at WP-02; audit monthly-partition automation deferred (default partition) |
| WP-02 | RBAC & staff admin | **DONE 2026-06-10** | `build/wp-02-rbac-staff-admin` → merge `2091f7c` (feat `ce9f762`) | Local 37/37 (TS-R full matrix log+warn+deny sweeps; TS-U staff ×10) + lint/scans; CI 13/13 green | — | Staff lifecycle HIGH-audited w/ diffs; grants only via M13 (C4); dormant-grant alert; level caps [Proposed]; matrix export-compare CLI; argon2 params pinned. NC carried: matrix content awaits S8 sign-off; admin SPA screens consolidate at WP-07 UI |
| WP-03 | Settings & transition engine | **DONE 2026-06-10** | `build/wp-03-settings-transition-engine` → merge `4906fbc` | Local 51/51 (TS-U transitions GENERATED from config: 40+ active rules × allow/role/reason + fail-closed validators + no-redeploy config edits; TS-I settings invalidation proven) + CI 13/13 | — | Engine = platform service; owning modules write their own tables via apply callback (ADR-010). Rule CONTENT stays workshop-owned [NC DEC-005/006] |
| WP-04 | Customers | **DONE 2026-06-10** | `build/wp-04-customers` → merge (see git log) | Local 61/61 (TS-U dedup/read-logging; TS-S #6 merge+undo) + CI 13/13 | A1 applied in DDL | Soft-unique phones (family-share NC); merge re-link hook ready for WP-07 drafts; HTTP surface consolidates at WP-07; primary-phone ordering bug caught by partial index in test |
| WP-05 | Catalog | **DONE 2026-06-10** | `build/wp-05-catalog` → merge (git log) | Local 68/68 (TS-U allergen-resolver + mirror-mode DoD) + CI 13/13 | **A6** | Mirror mode proven (import-only core writes until cutover_catalog); cycle guard (C7); routing rules zero-row [NC DEC-006]; catalog owner [NC Q1] |
| WP-06 | Import tooling | **DONE 2026-06-10** | `build/wp-06-import-tooling` → merge (git log) | Local 74/74 (TS-M real: dry-run report, apply gate, idempotency, quality gates, rollback) + CI 13/13 (ts-a rerun after Docker-registry flake) | — | Boundary scan caught 3 ADR-010 violations in first draft → import APIs on owning modules. Real legacy runs still blocked on access/export (WP-13) |
| WP-07 | Intake & WhatsApp panel | **DONE 2026-06-10** | `build/wp-07-intake-whatsapp-panel` → merge `59dfa3c` (feat `074417f`) | Local 83/83 full Vitest + lint/typecheck/build/scans; CI run 27289272120: **13/13 jobs green** | A7, A8 | M01 draft intake/completeness/incomplete queue/allergy warnings + M17 immutable manual WhatsApp refs. Built under ASM-003..024; sponsor review still required for assumptions. |
| WP-08 | Review queue | **DONE 2026-06-10** | `build/wp-08-review-queue` → merge `f68177d` (feat `c22885f`) | Local 93/93 full Vitest + lint/typecheck/build/scans; CI run 27290329062: **13/13 jobs green** | A9 | M02 queue/SLA/claim/decisions, append-only review decisions, return/reject via M01 transition port, approval emits `order.approved` with WP-09 conversion pending. ASM-025 reviewer=creator allow+audit remains sponsor-review-required. |
| WP-09 | Order core | **DONE 2026-06-10** | `build/wp-09-order-core` → merge `5e7bdd9` (feat `82d2121`) | Local 105/105 full Vitest + lint/typecheck/build/scans; CI run 27293379729: **13/13 jobs green** | A10 | M03 OrderFactory converts approved drafts, freezes package/item/allergen/address snapshots, generates fulfillment days, runs plan/day transitions via `transition_config`, supports end-date change requests and exception cases. Built under ASM-026/027 and ASM-043..046; sponsor review still required. |
| WP-10 | Kitchen | **DONE 2026-06-10** | `build/wp-10-kitchen` → merge `df5441d` (feat `35c3b37`) | Local 115/115 full Vitest + lint/typecheck/build/scans; branch CI run 27294417531: **13/13 jobs green**; merge CI run 27294563551: **13/13 jobs green after rerun of infra-only Docker pull flake** | A11 | M08 kitchen ticket generation, board, shared-device ticket transitions, day rollups, pack confirmation, allergy markers, unrouted tickets, and escalations. Built under ASM-028..030 and ASM-047..048; sponsor review still required. |
| WP-11 | Payments-lite | **DONE 2026-06-10** | `build/wp-11-payments-lite` → merge `f551a77` (feat `162ebe9`) | Local 128/128 full Vitest + lint/typecheck/build/scans; branch CI run 27295496661: **13/13 jobs green**; merge CI run 27295575948: **13/13 jobs green** | A12 | M07 payment records, manual link-sent capture, finance status-review queue, FI-only payment decisions via `transition_config`, HIGH audit/outbox, masked payment reads, legacy-unmapped queue port, and refund `not_enabled` stubs. Built under ASM-019..022, ASM-031..032, ASM-049; sponsor review still required. |
| WP-12 | Notifications & reports | **DONE 2026-06-10** | `build/wp-12-notifications-reports` → merge `9768842` (feat `1136886`) | Local 138/138 full Vitest + lint/typecheck/build/scans; branch CI run 27296556204: **13/13 jobs green**; merge CI run 27296643148: **13/13 jobs green** | A13 | M11 internal/email-capable notification templates, configurable trigger map, append-only/deduped notification log, and router; M15 intake funnel/daily ops/kitchen day-list projections, audited JSON export, and replay-equality harness. Customer notification channels remain dormant under ASM-033; ASM-034..035 remain sponsor-review-required. |
| WP-13 | Bridge & cutover tooling | **DONE 2026-06-10** | `build/wp-13-bridge-cutover` → merge `369266f` (feat `afb15e8`) | Local 147/147 full Vitest + lint/typecheck/build/scans; branch CI run 27297504061: **13/13 jobs green**; merge CI run 27297588422: **13/13 jobs green** | A14 | M18 reconciliation records/diffs/alerts and cutover flag audit; M19 active-plan importer, synthetic dry-run/apply/rollback, API surface, owner-module import/rollback ports, unknown payment status finance queue, unverified off-days/address markers. Real legacy apply still needs access/export; ASM-036..038 and ASM-050 remain sponsor-review-required. |
| WP-14 | Pilot hardening & gate | **BLOCKED** | — | — | — | **Hard entry gate per DEC-014 not met:** staging not provisioned/live/smoke-tested — **credential-blocked only since 2026-06-11** (deploy blockers D1–D6 fixed + CI-validated; checklist + smoke runbook executable end-to-end once sponsor supplies STG-1..6; readiness matrix in `wp14_blocker_report.md`). Workshop fully applied/no NC-carry remains a WP-14 requirement. |

Status vocabulary: NOT STARTED · IN PROGRESS (branch open) · BLOCKED (gate/NC — name it) · DONE (merged, suites green, pushed).

## Run log

| Date | Mode | Result |
|---|---|---|
| 2026-06-10 | Mode A — WP-00 | DONE (merge `e704eca`); suites green locally; staging deferred (region NC); WP-01 declared BLOCKED |
| 2026-06-10 | **Sprint Mode** (first run, after Mode B added @ `1ac4876`) | STEP 0 re-verified live: ①③ ✅, ② ❌, ⑤ ❌, ④ ◐. WP-00 already DONE (artifacts verified). Next eligible per dependency diagram = WP-01 → entry gate fails (② DEC-003, ⑤ workshop/NC-carry) → sprint stopped honestly, **0 new WPs executed**, no eligible WPs remain. Unblock = sign DEC-003 + record NC-carry note (or hold workshop) + PG-region note. |
| 2026-06-10 | Sponsor unblock pack (docs only) | DEC-003 SIGNED; NC-carry acceptance recorded (WP-01–06); interim PG staging region noted (AWS me-south-1). Gate re-check: ①②③⑤ ✅, ④ ◐ (CI ✅; staging not provisioned). **WP-01 still blocked on ④ staging-live only.** No WP executed (per session mission). |
| 2026-06-10 | Sponsor DEC-014 staging gate re-scope (docs only) | Staging-live moved to hard pre-pilot/WP-14 entry for WP-01–06; build proceeds on local + CI (CI mandatory). Gate re-check: all gates pass for WP-01–06 → **WP-01 ELIGIBLE**. Staging remains NOT provisioned (credentials blocker stands for provisioning only). No WP executed (per session mission). |
| 2026-06-10 | **Sprint Build Mode** (second run) | **WP-01→02→03→04/05→06 ALL DONE in one session** — 6 atomic WPs, each branch→scope→suites→green-CI→merge→register→push. 74 tests (12 files) green; 6 CI runs 13/13 (one ts-a Docker-registry flake re-run, never merged past red). Local PostgreSQL 16 installed for DB-backed suites. Amendment A6 raised (ingredient_allergen). Boundary scan caught 3 real ADR-010 violations mid-sprint (fixed via owning-module import APIs). Sprint stopped honestly at WP-07's workshop NC (stop-rule; NC-carry ends at WP-06). |
| 2026-06-10 | Discovery Mode - Legacy-first section alignment audit | Audit created at `19_Roadmap/legacy_first_section_alignment_audit.md`. Result **PASS_WITH_GAPS**: old-system section evidence is sufficient for a baseline map, current build coverage is foundation-heavy through WP-06, and WP-07 remains blocked on the mandatory intake-field set/workshop S3 Q8. No WP statuses changed; no feature code/tables/APIs/UI created. |
| 2026-06-10 | Discovery Mode - WP-07 `/orders/create` legacy review pack | Pack created at `22_Meeting_Notes/WP07_orders_create_legacy_review_pack.md`. Result **READY_FOR_SPONSOR_SIGNOFF**: old `/orders/create` field categories are documented, but submit blockers/defaults/warnings/customer identity/WhatsApp/allergy/payment/coupon/delivery decisions remain missing. WP-07 stays **BLOCKED**; no feature code/tables/APIs/UI created. |
| 2026-06-10 | Assumption-carry setup for WP-07+ | `ASSUMPTION_REGISTER.md`, `20_Decisions/NOTE_assumption_carry_wp07_plus.md`, and `22_Meeting_Notes/sponsor_review_package_unresolved_business_questions.md` created from sponsor/user directive. Result: WP-07 is eligible under explicit Assumed-for-build values; unresolved questions remain sponsor-review-required. No feature code/tables/APIs/UI created in this doc-only step. |
| 2026-06-10 | **Sprint Build Mode** (WP-07) | **WP-07 DONE** — branch `build/wp-07-intake-whatsapp-panel`, feature `074417f`, merge `59dfa3c`; local full Vitest 83/83 plus lint/typecheck/build/scans green; GitHub CI run 27289272120 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-08 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-08) | **WP-08 DONE** — branch `build/wp-08-review-queue`, feature `c22885f`, merge `f68177d`; local full Vitest 93/93 plus lint/typecheck/build/scans green; GitHub CI run 27290329062 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-09 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-09) | **WP-09 DONE** — branch `build/wp-09-order-core`, feature `82d2121`, merge `5e7bdd9`; local full Vitest 105/105 plus lint/typecheck/build/scans green; GitHub CI run 27293379729 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-10 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-10) | **WP-10 DONE** — branch `build/wp-10-kitchen`, feature `35c3b37`, merge `df5441d`; local full Vitest 115/115 plus lint/typecheck/build/scans green; branch CI run 27294417531 13/13 green; merge CI run 27294563551 13/13 green after rerun of an infra-only Docker pull flake in `ts-m`. Built under `ASSUMPTION_REGISTER.md`; WP-11 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-11) | **WP-11 DONE** — branch `build/wp-11-payments-lite`, feature `162ebe9`, merge `f551a77`; local full Vitest 128/128 plus lint/typecheck/build/scans green; branch CI run 27295496661 13/13 green; merge CI run 27295575948 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-12 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-12) | **WP-12 DONE** — branch `build/wp-12-notifications-reports`, feature `1136886`, merge `9768842`; local full Vitest 138/138 plus lint/typecheck/build/scans green; branch CI run 27296556204 13/13 green; merge CI run 27296643148 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-13 becomes next eligible. |
| 2026-06-10 | **Sprint Build Mode** (WP-13) | **WP-13 DONE** — branch `build/wp-13-bridge-cutover`, feature `afb15e8`, merge `369266f`; local full Vitest 147/147 plus lint/typecheck/build/scans green; branch CI run 27297504061 13/13 green; merge CI run 27297588422 13/13 green. Built under `ASSUMPTION_REGISTER.md`; WP-14 is now BLOCKED on hard staging-live/smoke-tested entry gate. |
| 2026-06-11 | **Independent review** (Claude, WP-01..13 audit) | **PASS_WITH_FIXES** — branch `review/claude-wp01-wp13-audit`; full report `19_Roadmap/wp01_wp13_independent_review.md`. 15 confirmed issues fixed with tests (1 critical: M01 date off-by-one east of UTC; high: missing read-path masking, kitchen rollup write-skew, end-date-reduction kitchen guard, ADR-010 rollback/read ports, non-transactional dry-run report). Suites grew 147→163, all green + scans/build. 7 findings logged-only (workshop/pre-pilot scope), incl. no-op transition validators and the cancel-cascade engine bypass — both pre-WP-14 items. Amendment A15. |
| 2026-06-11 | **WP-14 staging-readiness prep** (docs only) | **WP-14 still BLOCKED — honest stop, nothing deployed/marked done.** Verified live: zero cloud credentials anywhere (repo sweep + env), Docker absent locally, staging unprovisioned; region note ✅ (me-south-1 interim). Authored `16_Deployment/staging_provisioning_checklist.md` (sponsor inputs STG-1..7, verified env surface, pre-deploy defects D1–D6 — admin-SPA proxy gap, Secure-cookie/TLS, migrations-not-in-image, dead SESSION_SECRET, no deploy job, compose unvalidated — deploy sequence + 10-step smoke runbook) and `19_Roadmap/wp14_blocker_report.md` (29-item readiness matrix: 4 done / 10 possible-now / staging+workshop+sponsor blocked). Baseline re-verified on `main` `bd51afe`: 163/163 + scans + CI 13/13. Single unblock: sponsor supplies STG-1..6. |
| 2026-06-11 | **WP-14 deploy blockers D1–D6 fixed** (same PR #2) | All six fixed without credentials, none invented: D1 nginx SPA-fallback + same-origin API proxy (18 prefixes, `/kitchen` page vs `/kitchen/*` API collision handled) baked into the admin image; D2 TLS posture explicit + `trust proxy` + unit-tested `COOKIE_SECURE` override (defaults unchanged, fail-secure); D3 `migrate` image stage + compose `tools` service for the gated migration/bootstrap steps; D4 `SESSION_SECRET` struck (never read by code); D5 gated `deploy-staging` workflow (GHCR publish on built-in token; deploy job skipped till sponsor vars/secrets, loud-fail till STG-1 chosen); D6 CI `docker-validate` job (compose config + nginx `-t` + full image builds — first-ever validation of the WP-00 Docker artifacts, CI now 14 jobs). Suites 163→164 (cookie-flag test). Nothing deployed; WP-14 not marked complete; staging still needs sponsor inputs STG-1..6 only. |
| 2026-06-12 | **Sprint Build Mode (WP-UI-01)** | **WP-UI-01 DONE pending PR #3 merge** — branch `build/wp-ui-01-shell-login`: login page + central 401 fallback, app shell w/ sidebar (10 sections), SPA routes under `/app/*` (D1-safe), kitchen board preserved, read-only drafts/review-queue/orders lists. 3-lens adversarial review found 3 must-fix (session-expiry error walls, silently-swallowed logout failure on shared tablets, pushState back-button trap) — all fixed pre-push. CI 14/14 green (run 27421998444). Deployed to staging; visible Playwright suite 7/7 green (`tools/e2e-staging/tests/wpui-shell.spec.ts`). Next: WP-UI-02 (intake form, review decisions, orders detail, payment queue). |
| 2026-06-12 | **Staging provisioned + deployed — gate ④ staging-half flipped ✅** | Sponsor provided a VPS (Ubuntu 24.04, 8c/24GB; `20_Decisions/NOTE_vps_staging_host.md` supersedes me-south-1 for staging). Provisioned via new SSH MCP server (`tools/vps-mcp/`): Docker 29.5.3 + compose v5.1.4, ufw 22/80/443. New `docker/compose.staging.yml` (localhost-rebound ports — Docker published ports bypass ufw — secret-store PG password, restart policies, Caddy service) + `docker/Caddyfile` (Let's Encrypt on `13-140-159-201.sslip.io`). Deployed `main` `d69a107`: images built on-host, 13/13 migrations, super admin bootstrapped (idempotent script verified). **D7 found live + fixed:** nginx implicit trailing-slash 301 broke bare `/kitchen` (SPA board) → exact-match location added to `nginx.admin.conf`, rebuilt, retested (checklist §3). **Smoke 10/10 PASS** (checklist §6 evidence). Nightly `pg_dump` cron + first dump verified. Remaining WP-14 entry items: restore drill, workshop/sponsor items per `wp14_blocker_report.md` §4. |
| 2026-06-13 | **Legacy core coverage audit** (Review Mode, docs only) | Sponsor-directed evidence-only audit of the 7 legacy core modules vs current implementation. New `19_Roadmap/Legacy_Core_Coverage_Matrix.md` (per-module backend/API/UI/staging/cutover status, file-cited) + `Legacy_Core_Gap_To_Cutover.md` (shortest-path gap list + prioritized build list + WP sequence). Findings: Orders=B (backend+API done, action UI missing); Customers/Packages/Products/Reports/Settings=C (services done but Customers/catalog HTTP-controller-less, masters routes missing, finance-report parity + content/social/push out of MVP); Subscribers=D (marketing list, off the order-ops path). No module class A. Recommended sequence: **WP-API-01 → WP-UI-02 → WP-UI-03 → WP-UI-04**; sponsor-owned parallel = legacy export access + workshop pack. No WP executed. |
| 2026-06-13 | **WP-UI-03c settings/masters DONE** (Sprint Mode) | First WP-UI-03c sub-unit: settings/masters admin (`app/apps/admin/src/pages/Settings.tsx`; Settings sidebar flipped live + `/app/settings`). Masters tab — view + add area / delivery-slot / delivery-method / kitchen-section (`GET`/`POST /settings/masters/:kind`); Reason-codes tab — view + add by domain (`GET`/`POST /settings/reason-codes`). Reads `settings.read`, adds `settings.update.ops`. Settings-registry / feature-flag (gate-key) editing deliberately out of scope. PR #16 merge `c490bbd`; CI green (incl. ts-c/ts-i settings 17/17); admin tsc/eslint/build clean. Deployed (api **and** admin rebuilt on the VPS); **visible Playwright 1/1** (`wpui-settings.spec.ts`: view seeded area, add area live 201, switch kind to slots, reason-codes tab, add reason code live 201, zero client crashes). **Real backend bug caught + fixed (`0f42161`):** `POST /settings/reason-codes` was a dead route (404) — the generic `@Post(':key')` update route was declared before it, so Nest's in-order matching shadowed it (`key='reason-codes'` → unknown_key). Moved the static route ahead of `:key`; the endpoint had been unreachable since WP-API-01 (tests hit the controller method directly, not HTTP routing). Next: WP-UI-03c remaining (exceptions view / staff+RBAC+audit / dashboard cards). |
| 2026-06-13 | **WP-UI-03b reports DONE** (Sprint Mode) | Third WP-UI-03 sub-unit (second 03b screen): reports admin (`app/apps/admin/src/pages/Reports.tsx`; Reports sidebar flipped live + `/app/reports`). Read-only over M15 projections — intake-funnel / daily-ops / kitchen-day-list (`GET /reports/:name`, each `report.view.*`) + JSON export (`POST /exports`, `report.export`, client-side download). Reports rebuild from the outbox each call; empty sections render as a labelled demo-data gap. PR #13 merge `4dd5a3b` (screen) + **PR #14 merge `1408f60` (fix)**; CI green both; admin tsc/eslint/build clean. Deployed; **visible Playwright 1/1** (`wpui-reports.spec.ts`: intake-funnel + export + daily-ops + kitchen, asserts zero client crashes). **Real bug caught by the e2e + fixed:** switching report tabs briefly paired the new `report` with the previous report's data shape → `undefined.toLocaleString()` white-screened the whole SPA (no error boundary); fixed by keying loaded data to its request + non-number value guards. intake-funnel shows live seeded numbers (4 drafts created, 1 approved, by_channel phone:4). Next: WP-UI-03c. |
| 2026-06-13 | **WP-UI-03b DONE** (Sprint Mode, post-seed) | Second WP-UI-03 sub-unit: catalog browse admin (`app/apps/admin/src/pages/Catalog.tsx`, new `/app/catalog` route + sidebar). Read-only over the M05 read API (`GET /catalog/*`, `catalog.read`): Products tab (active-only filter) → detail with nutrition + resolved allergens; Packages tab → detail (duration / meals-per-day / parent); Masters tab over the 6 catalog kinds. **Read-only by design** — catalog stays import-only until `cutover_catalog` flips (mirror mode). PR #11 merge `955c9f4`; CI 14/14 + CodeRabbit pass; admin tsc/eslint/vite-build clean. Deployed (admin image rebuilt on the VPS via the `nutrezee-vps` MCP + `up -d`); **visible Playwright 1/1** (`wpui-catalog.spec.ts`: products→detail→packages→detail→masters, real, against the seeded catalog — first 03b screen demonstrable thanks to the seed). One test-selector fix (`getByText('Nutrition')` substring-matched the empty-state copy → `{exact:true}`). Next: WP-UI-03c (settings/masters admin, exceptions, staff/RBAC + audit, dashboard) and/or reports. |
| 2026-06-13 | **Staging seeded for UAT** (Deployment/UAT Mode, user-approved) | Resolved the cross-cutting staging-data gap — the intake→review→order→payment chain was empty because no catalog meant no submittable draft. Created a dedicated `uat-seed@nutrezee.local` super-admin (+ `ops_manager` + `order_agent`: the transition engine checks per-transition `allowed_roles` with **no super_admin bypass**, so super_admin alone can't submit a draft / approve / create an order) via the sanctioned `scripts/bootstrap-admin.mjs` in the migrate container — the user's real account was untouched. Seeded over HTTPS via the API: catalog (2 meal_types, 2 products, 2 packages) through the **M19 import** (`/imports/catalog/dry-run`→`/apply`; `via='import'` bypasses mirror mode), ops masters (1 area / slot / method) via `/settings/masters/:kind`, 1 customer + address, then a live chain → 1 order + auto `payment_record` + 1 payment-review waiting, plus 1 review-queue waiting + 2 open drafts. All four daily-ops screens now have live data. `cutover_catalog` still false. Details in memory `staging-uat-seed-data`. |
| 2026-06-13 | **WP-UI-03a DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | First WP-UI-03 sub-unit: customers admin (`app/apps/admin/src/pages/Customers.tsx`, new `/app/customers` route + sidebar — Customers was missing from nav). Search by phone, guided-create (dup block on `duplicate_phone`, warn/force on `possible_duplicate`), profile view (PII/HEALTH masked server-side + masked badge), edit name/email/dob/notes, phones/addresses/allergies display. Replaces legacy `/users/list/3` + customer search in `/orders/create`; merge-review deferred to WP-API-02. PR #10 merge; CI 14/14 (run 27465852463); admin checks clean. Deployed; **visible Playwright 1/1 FULL end-to-end** (`wpui-customers.spec.ts`: create→edit→search, real — first admin screen demonstrable without seed data). Next: WP-UI-03b (catalog read screens, or reports). |
| 2026-06-13 | **WP-UI-02d DONE → WP-UI-02 COMPLETE** (Sprint Mode via "Continue Nutrezee OS Agent") | Final WP-UI-02 sub-unit: Finance payment review queue (`app/apps/admin/src/pages/Payments.tsx`, `/app/payments` flipped live in the sidebar). Queue list (`GET /payment-reviews`, state filter) → decide: approve/confirm with transaction_ref + evidence note, or reject with a `payment_fail` reason code + note (`POST /payment-reviews/:id/decisions`); refunds stay `not_enabled` (WF-13, never auto-confirm). PR #9 merge; CI 14/14 (run 27465680346); admin checks clean. Deployed; **visible Playwright 3/3** (`wpui-payments.spec.ts`). **WP-UI-02 done — all four daily-ops roles now have their screen** (order agent=intake, Ops Manager=review+orders, kitchen=board, Finance=payment queue). Next: WP-UI-03 (admin parity: customers/catalog/reports/settings/staff/audit) — but the highest-value next step is seeding demo data so the intake→review→order→payment chain is clickable for UAT (queue cross-cutting note). |
| 2026-06-13 | **WP-UI-02c DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | Third WP-UI-02 sub-unit: order detail + lifecycle actions (`app/apps/admin/src/pages/Orders.tsx`, `/app/orders` upgraded from read-only list to action screen; superseded `OrdersPage` removed from lists.tsx). Open order → summary (PAYMENT-masked total) + fulfillment-days table; change status (config engine via `POST /orders/:id/transitions`, cancel pulls cancellation reason codes), change request (new end_date diff, WF-15), raise exception (WF-14 taxonomy, allergy_incident auto-HIGH). UI-only — M03 + reason-code APIs already live; server validates every transition/decision. PR #8 merge; CI 14/14 (run 27459064518); admin typecheck/lint/build clean. Deployed; **visible Playwright 3/3** (`wpui-orders.spec.ts`). Full open/act needs an order on staging (seed-data gap, tracked). Next: WP-UI-02d (payment review queue, Finance). |
| 2026-06-13 | **WP-UI-02b DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | Second WP-UI-02 sub-unit: Ops Manager review-queue actions (`app/apps/admin/src/pages/Review.tsx`, `/app/review-queue` upgraded from read-only list to action screen; superseded `ReviewQueuePage` removed from lists.tsx). Queue list → open draft → claim (`POST /review-queue/:id/claim`) → approve (per-warning override reasons) / return / reject with reason code + note (`POST /drafts/:id/decisions`). API-before-UI prerequisite shipped same branch: `GET /settings/reason-codes?domain=` + `SettingsService.listReasonCodes` (reject=rejection, return=return_to_draft domains; `settings.read`). Server enforces reason_code on reject/return + a reason per warning field to approve-past-warnings; UI mirrors both. PR #7 merge; CI 14/14 (run 27458674705); suite 192→193. Deployed; **visible Playwright 3/3** (`wpui-review.spec.ts`): queue loads, state filter, reason-code endpoint wired. Full claim/approve demo needs a submitted draft (staging seed-data gap, tracked). Next: WP-UI-02c (order detail) — or seed demo data to make the intake→review→approve chain clickable. |
| 2026-06-13 | **WP-UI-02a DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | First WP-UI-02 sub-unit: the intake draft form (`app/apps/admin/src/pages/Intake.tsx`, `/app/intake` + sidebar "New intake") — the order agent's daily screen, replaces legacy `/orders/create`. Channel + customer (find-by-phone/guided-create/unverified), package or items (`GET /catalog/*`), dates, area/slot/method (`GET /settings/masters/*`), payment method, address, WhatsApp ref panel; `POST /drafts` (idempotency-keyed) → `GET /drafts/:id` completeness panel → `POST /:id/submit`. PR #6 merge; CI 14/14 (run 27458274619); admin typecheck/lint/build clean. Deployed to staging; **visible Playwright 4/4** (`tools/e2e-staging/tests/wpui-intake.spec.ts`): renders, customer search, guided-create (201), draft create→completeness verdict, draft in list. **Known gap:** full happy-path *submit* needs catalog + ops-master seed data on staging (mirror mode blocks API catalog writes; area/slot/method zero-row until workshop) — create+completeness proven without it. Next: WP-UI-02b (review-queue actions). |
| 2026-06-13 | **WP-API-01b DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | Small API prerequisite for WP-UI-02, detected via the §B decision tree (API-before-UI): the intake draft form needs delivery-slot/method/area dropdowns but WP-API-01 added only POST for ops-masters. Added `GET /settings/masters/:kind` (kind whitelisted; `settings.read`, granted to order_agent so intake can load them; `?active=` filter) + `SettingsService.listMasters` (per-kind trusted column/order map, table name validated against it — kind never user-controlled in SQL). PR #5 merge `22bdcff`; CI 14/14 (run 27457952092); suite 190→192. Deployed + verified on staging (area/slot/method empty on fresh DB, section_master returns 5 seeds, unknown kind→400). **WP-UI-02 intake form now has zero API blockers.** |
| 2026-06-13 | **WP-API-01 DONE** (Sprint Mode via "Continue Nutrezee OS Agent") | First engineering-frontier unit. Branch `build/wp-api-01-...` → PR #4 merge `f9dcae6`. Three HTTP controllers over already-tested services, **no schema change**: M04 CustomerController (search/profile/create/update/address/allergy — PII/HEALTH masked at serialization per visibility grant, health grant-gated), M05 CatalogController (read-only products/packages/masters/allergens/nutrition; new read methods added to CatalogService — reads always allowed, mirror mode gates writes only; master `kind` whitelisted), Settings masters/reason-code POST routes. **3-lens adversarial review caught + fixed pre-merge: (1) HEALTH `diet_status_id` leaked unmasked on profile, (2) PII `full_name_en/_ar/notes`+address fields leaked, (3) 2nd-order SQL injection via `addMaster` column-name keys.** 26 new TS-C tests (incl. regression for all 3); suite 164→190; CI 14/14 (run 27454201633). Deployed to staging; **D8 found live + fixed** (`0c3af5a`): `/customers` + `/catalog` missing from the admin nginx proxy allow-list (D1-class) → SPA fallback instead of API; added both prefixes, rebuilt admin, re-verified (401 unauth + authed `{"items":[]}`). **Deferred (own unit): merge/undo** — MergeService has no app provider and its FK re-link steps are test-only. Fulfils WP-04 "HTTP surface consolidates at WP-07" + WP-03 masters-admin promises. Next: WP-UI-02. |
| 2026-06-13 | **Nutrezee OS Agent operating system created** (Review Mode, docs only) | Repo-level autonomous OS so Claude Code/Codex continue from project state without a new prompt each time. Standing command **"Continue Nutrezee OS Agent"**. Rewrote `AGENTS.md` + `00_AGENT_OPERATING_SYSTEM.md` (preserved source-of-truth table, binding constraints, forbidden-module list; added standing command, session-start sequence, frontier auto-detection, current state). New `19_Roadmap/NEXT_ACTION_QUEUE.md` (live next-work cursor — frontier WP-API-01→UI-02→UI-03→UI-04, sponsor-owned parallel track, deferred list), `19_Roadmap/AUTO_EXECUTION_RULES.md` (session-start, next-work decision tree, assumption logging, stop conditions, mandatory SESSION REPORT), `19_Roadmap/SPRINT_MODE.md` (six modes — Review/Build/Sprint/Fix/Deployment/UAT — + the autonomous loop). No code; no WP executed. Next: WP-API-01. |

## Amendments log (continues `phase_4_to_build_handoff.md` §4 — A1–A4 recorded there)

| ID | Date | Amendment | Raised by |
|---|---|---|---|
| A5 | 2026-06-10 | WP-00 "Environment standup" added ahead of WP-01 to satisfy global gate ④ (staging+CI) — resolves the bootstrap circularity in the Phase 4 gate; entry gate ①+③ only, no business code; staging provisioning conditional on a PG-region residency note in 20_Decisions/ (stop-rule carve-out); CI guards remain placeholders until WP-01 implements them | Phase 5 master prompt |
| A6 | 2026-06-10 | `ingredient_allergen` link table added (wave-2 catalog DDL) — AllergenResolver's derived_from_ingredient semantics required ingredient→allergen links missing from the logical data model; structure-only, discovered during WP-05 | WP-05 build session |
| A7 | 2026-06-10 | Assumption-carry control added for WP-07+ after sponsor/user directive: unresolved business decisions are captured in `ASSUMPTION_REGISTER.md`, assigned risk, marked sponsor-review-required, and allowed for implementation until revised; hard technical gates, dormant-module boundaries, real legacy access/export, and WP-14 staging gate still stop the build | Sponsor/user directive |
| A8 | 2026-06-10 | `draft_order.unverified_reason` added in WP-07 to carry the explicit unverified-customer submit justification required by ASM-005/006 without creating a separate review table before WP-08; structure-only, intake-owned, reversible if sponsor signs a different identity workflow | WP-07 build session |
| A9 | 2026-06-10 | WP-08 added a boundary-safe M02→M01 review port and `TransitionEngine.transitionInTx` so review decisions can atomically return/reject drafts without M02 writing `draft_order`; approve records an append-only review decision and emits `order.approved` with `conversion_pending_wp09` because OrderFactory belongs to WP-09 | WP-08 build session |
| A10 | 2026-06-10 | WP-09 added the M03 wave-4 order core tables and service ports: M03 owns `customer_order`, `order_item`, `fulfillment_day`, `order_status_history`, `change_request`, and `exception_case`; M03 reads approved review/customer/catalog/draft data only through owning-module APIs, then converts drafts via the M01 transition port. Added assumptions ASM-043..046 for continuous day generation, unverified-customer conversion block, exception taxonomy, and end-date-only change requests. Also fixed M03 date serialization to preserve PostgreSQL `date` calendar values under Asia/Kuwait timezone. | WP-09 build session |
| A11 | 2026-06-10 | WP-10 added the M08 wave-5 kitchen tables and service ports: M08 owns `kitchen_ticket`, `ticket_status_event`, `section_master`, and `escalation`; M08 reads order days/items through M03 and product routing through M05, generates idempotent section/unrouted tickets, records shared-device ticket status events, rolls fulfillment days through the status engine, supports pack confirmation, and raises/resolves kitchen escalations. Added assumptions ASM-047..048 for product-level routing scope and pack-confirmation override posture. | WP-10 build session |
| A12 | 2026-06-10 | WP-11 added the M07 wave-4 payment-lite tables and service ports: M07 owns `payment_record` and `payment_review_item`, uses the config-seeded payment machine for status changes, records manual link-sent events, queues finance payment reviews with evidence, keeps paid/failed/collected decisions FI-only, masks non-finance payment reads, exposes refund endpoints only as `not_enabled`, and provides a legacy-unmapped status queue port for WP-13. Added assumption ASM-049 for payment record creation timing and non-mutating payment reads. | WP-11 build session |
| A13 | 2026-06-10 | WP-12 added the M11/M15 notifications-and-reports slice: M11 owns `notification_template` and append-only `notification_log`, logs internal/email-capable notifications from configurable trigger-map rules, dedupes by source event/template/recipient, and keeps customer/WhatsApp/push/SMS notifications dormant; M15 owns no business tables and rebuilds intake funnel, daily ops, and kitchen day-list projections directly from `outbox_event`, with audited JSON exports and TS-E replay-equality coverage. | WP-12 build session |
| A14 | 2026-06-10 | WP-13 added the M18/M19 bridge-and-cutover slice: M18 owns append-only `reconciliation_run`, records reconciliation diffs with WARN audit/outbox on divergence, and wraps platform cutover-flag updates with HIGH audit; M19 adds active-plan batch-3 import through M03/M07 owner ports, synthetic dry-run/apply/rollback, persisted import reports, API endpoints, unmapped legacy payment statuses to finance review, and unverified off-day/address markers under ASM-036..038/050. Real legacy apply remains blocked on legacy export/access. | WP-13 build session |
| A16 | 2026-06-12 | **WP-UI added** (sponsor-directed in-session after seeing staging live): admin SPA screens over the existing API, staged UI-01 (login + shell + sidebar + kitchen + read-only lists) then UI-02+ (the WP-14 UAT minimum screen set). Resolves the plan's internal gap: Phase-6 UAT requires per-persona browser journeys (WF-01…08, 12–16) but WP-01–13 scoped only login-shell/role-UI-API/message-ref-panel/kitchen-board; the WP-01 login shell itself was never actually built (smoke logged in via curl). UI-only — no API changes authorized under this row. | Sponsor (chat, 2026-06-12) |
| A15 | 2026-06-11 | Independent-review corrections (see `wp01_wp13_independent_review.md`): migration `0013` adds the schema-mandated `payment_record(order_id)` index and removes the structurally-dead `dormant_role_granted` notification trigger (security events are audit-only, never on the outbox — the dormant-grant alert stays the WP-02 HIGH audit event, partially revising ASM-034's trigger list); M02 gains the missing `review.sla_alert` emitter the WP-12 trigger map already routed; M04/M05 gain M19 owner-rollback ports so import rollback no longer writes foreign tables; M19 apply/rollback now emit `bridge.import_run` to the outbox per module spec. | Review session |
| *next: A16* | | | |
