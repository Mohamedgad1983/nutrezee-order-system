{
  "name": "nutrezee-order-system",
  "private": true,
  "version": "0.0.1",
  "description": "Nutrezee Order System MVP — monorepo per DEC-011 (NestJS modular monolith + React/Vite). WP-00 scaffold; business modules arrive WP-01+.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces",
    "lint": "eslint .",
    "test:placeholder": "vitest run",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
