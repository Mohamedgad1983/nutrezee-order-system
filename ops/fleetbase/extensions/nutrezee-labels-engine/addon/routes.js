import buildRoutes from 'ember-engines/routes';

// This engine is rendered inside Fleet-Ops order details. It intentionally exposes no standalone
// operations UI: ops.nutreeze.com remains the one and only administration surface.
export default buildRoutes(function () {});
